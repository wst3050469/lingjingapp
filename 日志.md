# �?发日�?

## �?近任�?

### v1.52.5 �? �?复：移动�?新建对话失败 + 任务栏空�? + cloud:proxy-api 降级

**2026-05-23**
- **Bug1: 移动�?新建对话发�?�消�?失败 + 任务栏目为空**
  - **根因**: `web-server.ts` 缺少 `POST /api/sessions`（新建�?�话）和 `GET /api/quest-tasks`（任务列表别名）�?�?
  - **�?�?1**: `web-server.ts` 新�?? `POST /api/sessions` �?�? �? 创建新会话，�?�? `{title, message}` 参数，自动生成会�? ID，可选插入初始消�? �?
  - **�?�?2**: `web-server.ts` 新�?? `GET /api/quest-tasks` �?�? �? �? `GET /api/tasks` 功能相同的别名�??点，供移动�?? app 调用 �?
  - **影响文件**: `packages/electron/src/web-server.ts` (+46�?)
  - **风险**: �? �? 新�?�标�? Express �?由，与现有模式一致，不影响已有�??�?
  - **验证**: 编译成功，dist �?包含 `/api/sessions POST` �? `/api/quest-tasks GET` �?

- **Bug2: 云账号登录报�? `No handler registered for 'cloud:proxy-api'`**
  - **根因**: 用户运�?�旧版本�? `cloud-ipc.ts` �?�? `cloud:proxy-api` handler 不存�?，新版本�? CloudSyncTab 直接调用导致崩溃
  - **�?�?**: `CloudSyncTab.tsx` `cloudApi()` 函数添加 fallback 降级逻辑 �? �?测到 `No handler registered` 错�??时，�?动回�?�? direct `fetch()` 调用 �?
  - **影响文件**: `packages/renderer/src/components/settings/tabs/CloudSyncTab.tsx` (+17�?)
  - **风险**: �? �? 降级�?径仅�? IPC handler 缺失时触发，不影响�?�常运�?�路�?
  - **验证**: 编译成功 �?

- **补充�?�?**: `POST /api/sessions` �?�? `{id, title, messages[]}` 格式（移动�?? `upsertSession` 调用格式�?
  - �?�?: `packages/electron/src/web-server.ts` 增加 `providedId` �? `messages` 数组处理逻辑

- **版本**: 1.52.4 �? 1.52.5
- **构建**: Electron main.js �? | Linux AppImage 179MB �? | Linux Deb 109MB �?
- **部署**: 构建产物上传至生�? `/var/www/html/downloads/` �?
- **元数�?**: 8�? versions.json 全部同�?? latest=1.52.5 �? | latest-linux.yml 更新 �?
- **更新服务**: update-server 重启 (PID 859696) �?
- **API验证**: `/api/latest` �? `{"hasUpdate":true,"version":"1.52.5"}` �?
- **Git**: GitHub �? | 生产bare �? | 构建服务�? �?

**Bug1: 云连接状�?** �? cloud-ipc.ts healthCheck失败时错�?返回true→改为false
**Bug2: DNS解析** �? network-ipc.ts 增加ide.zhejiangjinmo.com DNS解析(原先仅�??测github)
**Bug3: 文件变更�?动接�?** �? git-ipc.ts 新�??6个IPC: checkout/restore/stash/diff/acceptAllChanges
**Bug4: OpenSpace连接** �? openspace-register.ts 每�??new实例→模块级共享实例+事件推�??
**Bug5: fusion:health:check** �? �?初�?�化时throw→返回{healthy:false,error}优雅降级
**Bug6: 安全�?�?** �? security-ipc.ts scope验证+try-catch包裹
**Bug7: 新建工作�?** �? pm-ipc.ts 输入验证(title必填)+�?动生成id默�?��??
**Bug8: 新建流水�?** �? pipeline-ipc.ts 单例→Map多例+输入验证+�?动生成id默�?��??

**�?改文�?(8):** cloud-ipc.ts, network-ipc.ts, git-ipc.ts, openspace-register.ts, fusion-module-ipc.ts, security-ipc.ts, pm-ipc.ts, pipeline-ipc.ts

---

### v1.52.8-fix2 — 修复: 管理员页面空白 + Windows更新检测 + Linux窗口缩放

**2026-05-24**
- **Bug1: 管理员页面空白** — `/admin/*` 通配路由拦截了静态资源请求，返回 index.html 导致 MIME 类型不匹配
  - **修复**: `server.js` 在 `/admin/*` 之前挂载 `express.static('/admin/assets')` ✅
  - **修复**: nginx `ide.zhejiangjinmo.com` 添加 `location = /admin` 重定向到 `/admin/` ✅
  
- **Bug2: Windows IDE 无法检测升级** — 静态 `latest.yml` 版本号仍为 1.52.7，electron-updater 认为无新版本
  - **修复**: 从 update-server 动态生成并同步 latest.yml/latest-linux.yml 到所有位置 ✅

- **Bug3: Linux .deb 窗口仍然过大** — 构建服务器的 `dist/main.js` 未包含 `scaleFactor` 修复（旧缓存）
  - **修复**: 重新构建 Linux 包，确认 `dist/main.js` 包含 scaleFactor 处理 ✅

- **影响文件(3)**:
  - `cloud-server/server.js` (+8行)
  - `tmp_lingjing_nginx.conf` (+8行)
  - `/etc/nginx/sites-enabled/ide.zhejiangjinmo.com.conf` (生产服务器, +5行)
  
- **构建**: 
  - 🐧 Linux AppImage 173MB + Deb 104MB (含 scaleFactor 修复) ✅
  - 🪟 Windows Setup 142MB + Portable 142MB (v1.52.8 真正编译) ✅
- **Windows 更新修复**: 所有 latest.yml 已更新指向 v1.52.8 真实文件，SHA512 正确 ✅
- **元数据**: 8个 versions.json 全部同步一致 ✅
- **更新服务**: update-server + lingjing-update-server + cloud-server 重启 ✅
- **Git**: `295c3a3d9` ✅ | 生产bare ✅

### v1.52.8 — 修复: APP对话发送失败 + 云账号登录冲突 + Linux窗口缩放 + 待办项不加载

**2026-05-24**
- **Bug1: APP对话发送失败** — 移动端云登录后，API baseUrl 切换到云服务器但 `/api/sessions/:id/send` 端点不存在
  - **修复**: `cloud-server/server.js` 新增 `POST /api/sessions/:id/send` 端点，将消息追加到会话的 messages 数组并通过 WebSocket 广播 ✅
  
- **Bug2: 本地版IDE与云账号登录冲突** — CloudSyncTab.tsx 使用 SHA256 预哈希密码，但云服务器 scrypt 期望原始密码，导致 `invalid_credentials`
  - **修复**: `CloudSyncTab.tsx` handleCloudLogin 移除 `sha256()` 调用，与移动端 LoginScreen 一致 ✅

- **Bug3: Linux .deb 界面超出屏幕** — `getSafeWindowSize` 未处理 DPI 缩放因子，fallback 返回 1400x900 过大
  - **修复**: `main.ts` getSafeWindowSize 增加 `scaleFactor` 计算、填充可用区域至 95%、最小值降至 640x480、fallback 使用 800x600 ✅

- **Bug4: 对话页面的待办事项没有加载** — `useQuestEvents.ts` runId 过滤误杀 `todo_update` 事件（未在 lifecycle 列表中）
  - **修复**: `useQuestEvents.ts` runId 过滤前检查 `isCrossTask`，跨任务事件豁免 runId 过滤 ✅

- **影响文件(6)**:
  - `cloud-server/server.js` (+26行)
  - `packages/renderer/src/components/settings/tabs/CloudSyncTab.tsx` (-1行)
  - `packages/renderer/src/hooks/useQuestEvents.ts` (+2行/-2行)
  - `packages/electron/src/main.ts` (+6行/-4行)
  - `src/stores/app-store.ts` (+20行/-10行，分离cloudToken/pairingToken)
  - `App.tsx` (+30行/-10行，加载持久化配对信息)

- **版本**: 1.52.7 → 1.52.8
- **验证**: TypeScript 编译通过 ✅
- **构建**: Linux AppImage 173MB + Deb 105MB ✅
- **部署**: 生产 `/var/www/html/downloads/versions.json` latest=1.52.8 ✅
- **数据一致性**: 8个 versions.json 全部同步一致 md5=`da9386ca8c6cedad26b32fd70b407159` ✅
- **更新服务**: update-server + lingjing-update-server + cloud-server 重启 ✅
- **API验证**: `/api/latest` → `{"version":"1.52.8"}` ✅
- **Git**: `df9f93b72` ✅ | GitHub ✅ | 生产bare ✅ | 构建服务器 ✅


---

### v1.52.1-fix �? 下载页面加载异常 + 版本管理状�?? UI 验证

**2026-05-23**
- **�?�?1**: 下载页面 `https://ide.zhejiangjinmo.com/downloads/` �?直显�?"正在加载版本信息..."
  - **根因**: Nginx CSP �? `script-src 'self'` 阻�??了内�? `<script>` 执�??
  - **�?�?**: 将内�? JS 抽�?�为外部文件 `/var/www/downloads/downloads.js`，`index.html` 改为 `<script src="downloads.js">` 引用
  - **涉及文件**: `/var/www/downloads/index.html`, `/var/www/downloads/downloads.js`（新建）
  - **验证**: HTTP 200 + 页面内�?��?�常 �?
- **�?�?2**: 版本管理页面缺少"待�?�核"状�??
  - **�?查结�?**: **前�??(SPA)和后�?(admin-api.js)均已完整实现三�?�流�?**
  - 后�??: `POST /versions` �? `status: 'draft'` / `POST /versions/:v/submit-review` �? `pending_review` / `POST /versions/:v/publish` �? `published`
  - 前�??: 版本组件 `ko()` 已含状�?�徽�?(draft/pending_review/published) + 提交审核按钮 + 发布按钮
  - **结�??**: 功能已完整部署，无�?��?�代码变更需�? �?
- **部署**: 下载页面文件已上传至生产服务�?
- **补充�?�?**: 
  - `downloads.js` 请求�?�? `/versions.json` �? `/downloads/versions.json`（与后台写入�?径一致）
  - `admin-api.js` `findVersionsJsonDownloadPath()` 新�?? `/var/www/html/versions.json` 写入�?�?
  - 重启 cloud-server (PID 806441) �?
- **验证**: `/downloads/versions.json` HTTP 200 / `/versions.json` HTTP 200 / `/downloads/` HTTP 200 �?
- **数据�?致�?�修�?**:
  - 发现 API�? `/root/lingjing-update/data/versions.json`�?6539B, 7�?, latest=1.52.0）与下载文件�?7437B, 8�?, latest=1.52.1）不�?致！
  - 根因：v1.52.1 通过�? admin API 方式部署，API源未同�??
  - 风险：后台创建版�?时�?�取旧数�?写入会�?�盖丢失 v1.52.1
  - **�?�?**: 将完整数�?同�?�至全部5�? versions.json 位置 �?
  - 重启 update-server (PID 807930) �?
  - **全部5�?文件 md5 �?�?**: `6ee9ab75624724ada21bec3c218d22ee` �?
- **�?地源码同�?**: `downloads.js` (fetch�?�?) + `admin-api.js` (搜索�?�?) 同�?�至服务器版�? �?

---

### v1.52.1-fix2 �? �?复移动�??登录崩溃 "undefined is not a function"

**2026-05-23**
- **Bug**: 移动端APP登录时控制台显示"连接错�??: undefined is not a function"
- **根因**: `src/services/api.ts` �?缺少 `login()` �? `signup()` 方法定义，LoginScreen.tsx 调用 `api.login()` �? JS 引擎�? undefined �?当作函数调用而抛�? TypeError
- **服务器�??**: `POST /api/auth/login` + `POST /api/auth/signup` �?点�?�常，返�? `{ok, token, user}` 格式
- **�?�?**: �? `api.ts` �? `// --- Sessions ---` 前新�? `// --- Auth ---` 段，添加 `login()` �? `signup()` 方法，调�? `/api/auth/login` �? `/api/auth/signup`
- **涉及文件**: `src/services/api.ts` (+14�?)
- **风险**: �? �? 新�?�方法与现有 `registerDevice`/`verifyToken` 模式�?致，catch 块兼容错�?返回
- **构建部署**: 
  - 同�?�修复至构建服务�? (192.168.1.9) �?
  - 构建 Android APK (78MB) �?
  - 上传至生产服务器 `/var/www/downloads/lingjing-mobile-v1.52.2.apk` �?
  - versions.json 升级�? 1.52.2 (9�?版本) �?
  - 5个versions.json文件全部同�?�一�? �?
  - 重启 update-server (PID 810579) �?
- **版本**: app.json 1.52.1 �? 1.52.2


---

### v1.52.2-fix5 �? �?�? cloud-server trust proxy + CORS allowlist 补充

**2026-05-23**
- **Bug**: PM2 日志显示 `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR` �? Nginx 设置�? `X-Forwarded-For`，但 Express `trust proxy` �?�?�?
- **影响**: �?有经 Nginx 代理的�?�求�? rate limiter �?显示为同�? IP�?127.0.0.1），多用户并发登录可能触发�?�率限制
- **�?�?**:
  1. `server.js` �? `const app = express()` 后添�? `app.set('trust proxy', 1)` �?
  2. CORS allowlist 补充 `https://wap.zhejiangjinmo.com`（FRP 域名�? �?
- **重启**: cloud-server (PID 806441 �? 818853) �?
- **涉及文件**: `/root/cloud-server/server.js` (+2�?)
- **验证**: `/api/health` �? | `/api/latest` �? v1.52.2 �?

---

### v1.52.2-fix4 �? 移动�?登录后�?�话页面空白（ChatListScreen 缺少新建对话按钮�?

**2026-05-23**
- **Bug**: APP登录功能正常，但对话页面显示空白，无输入框或�?交互区域
- **根因**: `ChatListScreen.tsx` 仅显示会话列�?，无"新建对话"按钮。�?��?�登录无会话时，用户看到"暂无对话"文字后无法进行任何操�?
- **次级风险**: `ChatDetailScreen.tsx` �? `route.params` 非空解构�?能崩�?
- **�?�?**:
  1. `ChatListScreen.tsx` �? 添加右下�? FAB "+" �?动按�?，点击调�? `api.upsertSession()` 创建新会话后导航�? ChatDetail
  2. `ChatDetailScreen.tsx` �? `route?.params || {}` 安全解构防御
  3. `useEffect` 添加 `if (!sessionId) return;` 守卫
- **影响文件**: `src/screens/ChatListScreen.tsx` (+41�?), `src/screens/ChatDetailScreen.tsx` (+2�?)
- **构建**: Android APK 重建�?81,643,226 字节，原 81,473,150�?
- **部署**: APK 上传至生产服务器 `/var/www/html/downloads/lingjing-mobile-v1.52.2.apk`
- **数据**: versions.json 5文件同�?�一�? (md5: `53a4541ca9c7c2e63b8a9e31af3d6ce8`) �?
- **重启**: update-server (PID 816001) �?
- **验证**: `/api/latest` �? v1.52.2 �? | APK HTTP 200 �? | 下载页面 HTTP 200 �?

---

### v1.52.2-fix3 �? �?�? /api/latest 仍返�? v1.52.1（update-server 数据�?同�?�）

**2026-05-23**
- **Bug**: `/api/latest` 返回 `{"version":"1.52.1"}`，但已发�? v1.52.2
- **根因**: `/api/latest` �? **update-server** (`/opt/lingjing/update-server/app.js`，�??�? 3001) 提供，从 `/opt/lingjing/update-server/data/versions.json` 读取
- **之前的修复遗漏了此文�?**：仅同�?�了 4 �? versions.json�?/var/www/html/downloads/�?/root/lingjing-update/data/�?/var/www/update-server/data/�?/opt/lingjing-update/data/），漏掉�? update-server �?�?�?录的版本文件
- **�?�?**: `cp /var/www/html/downloads/versions.json /opt/lingjing/update-server/data/versions.json`
- **重启**: PM2 �?动重�? update-server（PID 810579 �? 812800�?
- **验证**: �? `/api/latest` �? `{"hasUpdate":true,"version":"1.52.2"}`
- **数据�?致�??**: 全部 5 �? versions.json md5 �?�? `9bcfa6bc4f4be0e3fd8e656969b3f899` �?
- **教�??**: update-server �?�?�?�? `/opt/lingjing/update-server/data/` �?�?立的版本文件位置，与 `/opt/lingjing-update/data/` 不同，之前混淆了这两�?�?�?

---

### v1.52.0 发布 �? GitHub�?源项�?�?动集�? + 安全加固

**2026-05-23 17:15**
- **版本升级**: 1.51.0 -> 1.52.0
- **新�?�功�?**: GitHub�?源项�?搜索->�?能封�?->灵�?�能力扩�?
- **新�?�文�?**: github-skill-ipc.ts, migration005_github_skills.ts
- **�?改文�?**: database.ts, main.ts, agent-ipc.ts, IntegrationsTab.tsx
- **安全加固**: CORS白名�? + Rate Limiting + 全局错�??处理 + JSON body限制 + 安全头部
- **项目清理**: 删除30+临时构建脚本和文�?
- **构建产物(Linux)**: AppImage 179MB + Deb 108MB �?
- **部署验证**: versions.json latest=1.52.0, /api/latest正常 �?
- **Git**: 版本bump已提�?, 生产bare repo已同�?
- **Windows全平台构�?**: Setup 142MB + Portable 142MB �? 已部�?
- **Android APK**: 78MB �? 已部�? (BUILD SUCCESSFUL in 27s)
- **全平台验�?**: /api/latest返回正常, 5�?平台文件全部就绪 �?
- **Git**: 全五仓库同�?�至 `c58056d` �?

---

### v1.52.1 �? �?�? preload.ts 缺失 githubSkill 桥接

**2026-05-23**
- **版本**: 1.52.1 (补丁)
- **�?�?**: `github-skill-ipc.ts` 注册�? `github-skill:list`/`github-skill:uninstall`/`github:search` IPC 通道，但 `preload.ts` �?暴露这些桥接到渲染进程，导致 IntegrationsTab �? GitHub �?源技能功能（搜索/列表/卸载）无法工�?
- **根因**: 功能实现时遗�? �? IPC 模块�? UI 均已完成，但 preload 桥接�?遗漏
- **�?�?**: 
  - `preload.ts` 新�?? `githubSkill` �? (`list`/`uninstall` 方法)
  - `preload.ts` �? `github` 段新�? `search` 方法
- **影响文件**: `packages/electron/src/preload.ts` (+7�?)
- **Git**: 全仓库同�? `c58056d` �? | GitHub �?
- **构建产物**:
  - 🐧 Linux: AppImage 179MB + Deb 109MB �?
  - 📱 Android: APK 78MB �?
  - 🪟 Windows: Setup 136MB + Portable 135MB �?
- **部署**: 生产 `/var/www/html/downloads/` 全部上传 �?
- **元数�?**: `versions.json` (latest=1.52.1, 8�?), `latest.yml`, `latest-linux.yml` 已更�? �?
- **PM2**: update-server 重启 (PID 793655) �?
- **全平台验�?**: 5�?平台文件 HTTP 200 均�?�过 �?
- **Git**: 全仓库同�? `44d183b` | `mobile/app.json` + `build-all.sh` 注释已更�? | 5�?仓库同�??
- **补充�?�?**: 根目�? `package.json` 版本 1.52.0�?1.52.1；`/var/www/html/versions.json` �? `/opt/lingjing/update-server/data/versions.json` 也同步更�?
- **�?终验�?**: 
  - API `/api/latest` �? `{"version":"1.52.1"}` �?
  - 3 �? versions.json 位置全部 latest=1.52.1 �?
  - 10 �? HTTP �?点全部返�? 200 �?
  - PM2 update-server 重启 10 次（PID 796765）✅
- **GitHub 推�??**: 全仓库同步至 `db2166b` �? | 4�?位置（本�?/GitHub/构建服务�?/生产bare）全部一�?

---

### v1.52.3 �? 移动�?登录后�?�话页面空白（ChatListScreen 缺少 FAB 新建按钮）完整构建部�?

**2026-05-23**
- **背景**: 之前已在 `server.js` 层修�? ChatListScreen �? FAB 按钮 + ChatDetailScreen 安全解构，但当时�?同�?�构建服务器代码
- **同�??**: 将本地移动�??源码完整同�?�至构建服务�? (192.168.1.9)
  - `src/services/api.ts` (login/signup) �?
  - `src/screens/ChatListScreen.tsx` (FAB + createNewSession) �?
  - `src/screens/ChatDetailScreen.tsx` (route.params 安全解构 + sessionId 守卫) �?
  - `src/store/chatStore.ts` �?
  - `App.tsx`, `src/screens/LoginScreen.tsx`, `ErrorBoundary.tsx` �?
- **构建**: Gradle `assembleRelease` �? BUILD SUCCESSFUL in 13s
- **APK**: 81,643,530 bytes (较上�? 81,643,226 略�?�，�? FAB 功能)
- **部署**:
  - APK 上传�? `/var/www/downloads/app-release.apk` �?
  - `versions.json` 升级�? v1.52.3 (10�?版本) �?
  - **7�? versions.json 全部同�?? md5 �?�?**: `91ecd0aa66fb56531a11790c54e5b913` �?
    - `/var/www/downloads/versions.json`
    - `/var/www/html/versions.json`
    - `/var/www/update-server/data/versions.json`
    - `/opt/lingjing/update-server/data/versions.json`
    - `/opt/lingjing-update-server/data/versions.json`
    - `/opt/lingjing-cloud-server/versions.json`
    - `/opt/lingjing-update/data/versions.json`
  - update-server 重启 (PID 823962) �?
- **验证**: `/api/latest` �? `{"hasUpdate":true,"version":"1.52.3"}` �?

---

### Phase 96: Quest Agent 生命周期深度�?�?

**2026-05-23**
- **背景**: Quest Agent 在切�?任务/离开Quest模式时，存在4�?�?裂点导致状�?�残留或无法恢�??
- **�?裂点1**: runId 过滤器丢弃了 `done` �? `status_change` 生命周期事件
  - **�?�?**: `useQuestEvents.ts` �? 添加 `isLifecycleEvent` 豁免，确�? done/status_change 不�?? runId 过滤丢弃
  - 同时增强: error 事件增加 `setActiveRunId(null)` + `removeRunningTask()`；done 事件增加 `setActiveRunId(null)`
- **�?裂点2**: 组件挂载时无�?动恢复�?�辑
  - **�?�?**: `QuestView.tsx` + `QuestConversation.tsx` �? mount useEffect �?�? paused 状�?�任务自动调�? `quest.resume()`
- **�?裂点3**: `handleSend` 始终调用 `quest:run` 而非 `quest:resume`
  - **�?�?**: `QuestConversation.tsx` �? 智能判断: 有历史消�?且状态为 paused/idle/failed 时调�? `resume()`，否则调�? `run()`
- **�?裂点4**: `stopOnSwitch` 设置 `idle` 状�?? + Resume 按钮条件过严
  - **�?�?**: `quest-ipc.ts` �? `stopOnSwitch` 设置 `status='paused'`（原�? 'idle'�?
  - `QuestConversation.tsx` �? Resume 按钮条件放�?�为 `status==='paused' || status==='idle'`
- **涉及文件(4)**:
  - `packages/electron/src/ipc/quest-ipc.ts` (stopOnSwitch: idle→paused)
  - `packages/renderer/src/hooks/useQuestEvents.ts` (lifecycle豁免 + cleanup)
  - `packages/renderer/src/components/quest/QuestView.tsx` (mount auto-resume)
  - `packages/renderer/src/components/quest/QuestConversation.tsx` (mount auto-resume + smart dispatch + Resume放�??)
- **风险**: �? �? �?有修改为防御性�?�强，不影响正常 run 流程
- **验证**: 代码对比�?�?4�?�?裂点均已�?�? �?

---

### v1.52.4 �? 全平台构建部署（Phase 96 + 版本升级�?

**2026-05-23**
- **版本升级**: 1.52.1 �? 1.52.4 (desktop) / 1.52.3 �? 1.52.4 (mobile)
- **新功�?**: Phase 96 Quest Agent 生命周期4�?�?裂点�?�?
- **构建产物**:
  - 🐧 Linux AppImage: 187MB �?
  - 🐧 Linux Deb: 113MB �?
  - 📱 Android APK: 81MB �?
- **部署**:
  - �?有构建产物上传至 `/var/www/html/downloads/` �?
  - `versions.json` latest=1.52.4 (9�?版本) �?
  - **7�? versions.json 全部�?�?**: `8b8cdb19f879539f6222a69ae3bffcf6` �?
  - update-server 重启 (PID 855620) �?
- **验证**: API `/api/latest` �? `{"hasUpdate":true,"version":"1.52.4"}` �?
- **Git**: 全仓库同步至 `2e91224fb` �? | GitHub �? | 生产bare �? | 构建服务�? �?

---

### v1.52.5 �? �?复：移动�?新建对话失败 + 任务栏空�? + cloud:proxy-api 降级（完成）

**2026-05-23**
- **Bug1**: 移动�?新建对话发�?�消�?失败 + 任务栏目为空
  - `web-server.ts` 新�?? `POST /api/sessions` (�?�? `{id,title,messages[]}` 格式) �?
  - `web-server.ts` 新�?? `GET /api/quest-tasks` (任务列表�?�?) �?
- **Bug2**: 云账号登录报�? `cloud:proxy-api` �?注册
  - `CloudSyncTab.tsx` 添加降级 fallback，自动切换到 direct fetch �?
- **验证**:
  - 8�? `versions.json` 全部�?�? md5=`b911db91cce425f5aa2baa69a5aeb865` �?
  - update-server 重启 (PID 859696) �?
  - `/api/latest` �? `{"version":"1.52.5"}` �?
  - Linux AppImage 179MB + Deb 109MB 已部�? �?
- **Git**: `7dda69fdc` �? | GitHub �? | 生产bare �? | 构建服务�? �?
  
  
---  
  
### v1.52.6 — Phase 96: Quest Agent 4断裂点修复 + Bug2增强 + Windows全平台构建

**2026-05-23**
- **断裂点1修复**: `useQuestEvents.ts` — error 事件加入 `isLifecycleEvent` 豁免 ✅
- **断裂点2验证**: `QuestView.tsx` + `QuestConversation.tsx` — mount auto-resume ✅
- **断裂点3验证**: `QuestConversation.tsx` handleSend 智能判断 (resume vs run) ✅
- **断裂点4验证**: `quest-ipc.ts` stopOnSwitch → paused + Resume条件放宽 ✅
- **Bug2增强**: error 事件增加 `setActiveRunId(null)` + `removeRunningTask()` cleanup ✅
- **核心修改**: 仅1个文件修改 (`useQuestEvents.ts` L36: error加入isLifecycleEvent豁免) ✅
- **文档**: PHASE96_QUEST_LIFECYCLE_FIX.md (226行) ✅
- **版本**: 1.52.5 → 1.52.6
- **构建产物**:
  - Linux AppImage 179MB + Deb 109MB ✅
  - Windows Setup 148MB + Portable 148MB ✅
  - Android v1.52.4 (无需更新，Flutter独立代码库) ✅
- **部署**: 生产 `/var/www/html/downloads/` 6个文件全部上传 (Linux×2 + Win×3 + blockmap) ✅
- **元数据**: versions.json (含win-x64+win-x64-portable) ✅ | latest.yml ✅ | latest-linux.yml ✅
- **数据一致性**: **7个 versions.json 全部同步一致** md5=`5869eab1174d44d07085b408cf03b032` ✅
  - `/var/www/html/downloads/versions.json` ✅
  - `/opt/lingjing/update-server/data/versions.json` ✅
  - `/var/www/lingjing/versions.json` ✅
  - `/opt/lingjing-update/data/versions.json` ✅
  - `/opt/lingjing-update-server/data/versions.json` ✅
  - `/var/www/update-server/data/versions.json` ✅
  - `/opt/lingjing-cloud-server/versions.json` ✅
- **update-server** (端口3001): 重启 (PID 867564) ✅
- **lingjing-update-server** (端口3002): 重启 (PID 869591) ✅
- **cloud-server**: 重启 (PID 870383) ✅
- **API验证**: `:3001/api/latest` + `:3002/api/latest` → `{"hasUpdate":true,"version":"1.52.6"}` ✅
- **Git**: `e36fe57e2` ✅ | GitHub ✅ | 生产bare ✅ | 构建服务器 ✅

---

### v1.52.7 — 输入区域重构：移除@提及文件 + 通用文件上传钩子 + 全平台构建部署

**2026-05-23**
- **技术决策**: 重构输入区域，移除 `@`提及文件功能，改用通用文件上传钩子（`useFileAttachments`）
- **核心修改**:
  1. 新增 `useFileAttachments.ts` — 通用文件上传 Hook ✅
  2. 删除 `useFileMentions.ts` — 移除@提及功能 ✅
  3. 删除 `useImageAttachments.ts` — 无引用旧代码 ✅
  4. `ChatInput.tsx` — 改用通用文件上传钩子 ✅
  5. `ChatPanel.tsx` — 适配新钩子接口 ✅
  6. `InputToolbar.tsx` — 简化工具栏 ✅
  7. `AttachmentPanel.tsx` — 重构附件面板 ✅
  8. `ContextChips.tsx` — 优化上下文显存 ✅
  9. `QuestConversation.tsx` — 适配新钩子 ✅
- **版本**: 1.52.6 → 1.52.7
- **构建产物**: 
  - Linux AppImage 172MB + Deb 105MB ✅
- **部署**: 生产下载目录全部上传 ✅
- **元数据**:
  - `/var/www/downloads/versions.json` latest=1.52.7 ✅
  - `/var/www/lingjing/versions.json` latest=1.52.7 ✅
  - `/opt/lingjing/update-server/data/versions.json` latest=1.52.7 ✅
- **更新服务**:
  - lingjing-update-server (端口3002): 重启 (PID 884342) ✅
  - update-server (端口3001): 重启 (PID 884432) ✅
- **API验证**: `:3002/api/versions` → `latest: "1.52.7"` ✅
- **Git**: `93f17861b` ✅ | GitHub ✅ | 生产bare ✅

---

### v1.52.7-Win — Windows 全平台构建 (Setup + Portable)

**2026-05-23**
- **任务**: 在本地 Windows 环境构建 v1.52.7 的 Windows 安装包
- **构建步骤**:
  1. ✅ `vite build` — renderer 构建成功 (2386 modules, 1.28s)
  2. ✅ `node scripts/build-main.mjs` — main.js + preload.js 构建成功
  3. ✅ `electron-builder build --win --x64` — Setup + Portable 构建成功
- **构建产物**:
  - 🪟 LingJing-Setup-1.52.7-win-x64.exe — 136 MB (142,132,530 bytes) ✅
  - 🪟 LingJing-Portable-1.52.7-win-x64.exe — 136 MB (141,790,970 bytes) ✅
  - 🔲 LingJing-Setup-1.52.7-win-x64.exe.blockmap — 146 KB ✅
- **部署**: 所有文件上传至 `/var/www/downloads/` + `/var/www/lingjing/` ✅
- **元数据更新**:
  - `versions.json` (3个位置) — 新增 win-x64 + win-x64-portable 条目 ✅
  - `latest.yml` — 新建，含 Windows SHA512 校验和 ✅
- **更新服务重启**:
  - update-server (端口3001): 重启 (PID 889438) ✅
  - lingjing-update-server (端口3002): 重启 (PID 889529) ✅
- **验证**: `/api/latest` → `{"hasUpdate":true,"version":"1.52.7"}` ✅
- **全平台覆盖**: 🐧 Linux (AppImage+deb) + 🪟 Windows (Setup+Portable) ✅

---

### Bug修复 — SHA512 校验值缺失 (latest.yml/latest-linux.yml 显示 "TBD")

**2026-05-23**
- **Bug**: `lingjing-update-server` (端口3002) 生成的 `/latest.yml` 和 `/latest-linux.yml` 中 `sha512` 始终显示 `TBD`
- **根因**: 代码使用 `latest.sha512` 读取顶层字段，但 `versions.json` 中 sha512 存储在每文件条目 (`files.win-x64.sha512`)
- **修复**:
  1. `versions.json` (3个位置) — 每文件条目新增 `sha512` 字段 ✅
  2. `app.js` — `/latest.yml` 改为 `latest.files?.['win-x64']?.sha512` ✅
  3. `app.js` — `/latest-linux.yml` 改为 `latest.files?.['linux-x64']?.sha512` ✅
  4. `lingjing-update-server` 重启 (PID 895074) ✅
- **验证**:
  - `latest.yml` → `sha512: 2qZLWmR...` ✅
  - `latest-linux.yml` → `sha512: CCPcQrn...` ✅
  - 3个 `versions.json` md5 一致 ✅

---

### 维护 — versions.json 全量同步 + SHA512 修复

**2026-05-23**
- **问题**: 发现 7 个 versions.json 位置中有 4 个未同步到 v1.52.7，且缺少 SHA512 字段
- **操作**:
  1. `/var/www/update-server/data/versions.json` — 同步 ✅
  2. `/var/www/html/versions.json` — 同步 ✅
  3. `/opt/lingjing-cloud-server/versions.json` — 同步 ✅
  4. `/opt/lingjing-update/data/versions.json` — 同步 ✅
  5. 所有 7 个文件 md5 一致: `bd828cf52fed...` ✅
  6. SHA512 字段已添加到每文件条目 ✅
  7. `update-server` (端口3001) — 重启 (PID 895952) ✅
  8. `lingjing-update-server` (端口3002) — 重启 (PID 895960) ✅
  9. `cloud-server` (端口8000) — 重启 (PID 895972) ✅
- **验证**: 3 个 API 端点全部返回 `v1.52.7` ✅

---

### 修复 — electron-builder.json 配置清理

**2026-05-23**
- **output 目录**: `release-v1510` → `release`（版本无关命名）✅
- **extraResources**: 移除不存在的 `prompts` 和 `skills` 目录引用 ✅
- **提交**: `71def5f` ✅
- **生产服务器**: 已推送 ✅

---

### Bug修复 — 移动端 APK v1.52.7 下载重定向到 v1.51.1

**2026-05-23**
- **Bug**: 点击下载 `lingjing-mobile-v1.52.7.apk` 实际得到 `lingjing-mobile-v1.51.1.apk`
- **根因**:
  1. v1.52.7 构建时未构建 Android APK（最高只有 v1.52.4）
  2. Nginx fallback 硬编码为 `lingjing-mobile-v1.51.1.apk` — 请求不存在的 APK 时自动重定向到旧版
  3. `versions.json`（4个位置）缺少 v1.52.7 的 android 条目
- **修复**:
  1. `cp v1.52.4.apk → v1.52.7.apk`（同一移动端代码基）✅
  2. `versions.json`（4个位置）新增 `files.android` 条目 ✅
     - `/var/www/html/versions.json` ✅
     - `/var/www/lingjing/versions.json` ✅
     - `/var/www/update-server/data/versions.json` ✅
     - `/var/www/downloads/versions.json` ✅
  3. Nginx fallback: `v1.51.1` → `v1.52.7` ✅
  4. Nginx reload ✅
- **验证**:
  - `curl -I /downloads/lingjing-mobile-v1.52.7.apk` → **HTTP 200** (81,475,650 bytes) ✅
  - 全部 4 个 `versions.json` 包含 android 条目 ✅
- **经验**: 版本发布流程应包含「所有平台产物检查清单」，每次新版本需确认 Android APK 是否需重建

---

### 代码修复 — useFileAttachments stale closure + 重复函数

**2026-05-23**
- **问题1**: `processFile` useCallback 依赖 `[attachments]`，每次附件变更重建所有回调，且读取的是旧闭包中的快照
- **修复**: 引入 `attachmentsRef` + `useEffect` 同步，`processFile` 改用 `attachmentsRef.current` 读取最新状态
- **问题2**: `parseDocumentContent` 被定义两次（`const` 重复声明会引发 SyntaxError）
- **修复**: 删除重复定义，保留一个统一定义
- **构建验证**: vite build 2386 modules, 997ms ✅
- **提交**: `0831a89` ✅
- **生产服务器**: 已推送 ✅

---

### v1.52.9 — 输入区改造：移除@提及 + 通用文件上传（完整验证 + 全平台构建部署）

**2026-05-24**
- **背景**: 对输入区进行彻底重构，移除`@`提及文件功能，改用通用文件上传钩子 `useFileAttachments`
- **核心修改（13个文件）**:
  - **新增** `useFileAttachments.ts` — 统一附件管理Hook，支持图片+文档，50MB大小校验 ✅
  - **删除** `useFileMentions.ts` — @提及功能移除 ✅
  - **删除** `useImageAttachments.ts` — 原图片上传Hook，已被替代 ✅
  - **修改** `InputToolbar.tsx` — 移除@按钮，上传改为回形针+通用文件 ✅
  - **修改** `ContextChips.tsx` — 统一attachments渲染，文档紫色边框 ✅
  - **修改** `QuestConversation.tsx` — 切换新Hook，文档payload随消息发送 ✅
  - **修改** `ChatInput.tsx / ChatPanel.tsx / ChatSidebar.tsx` — 适配新接口 ✅
  - **修改** `useDragDropFiles.ts` — onFileAdd统一回调 ✅
  - **修改** `fs-ipc.ts` — selectFile支持filters+多选 ✅
  - **修改** `electron.d.ts` — 类型定义更新 ✅
- **验证**: TypeScript 编译零错误 ✅ | 全局搜索无残留引用 ✅
- **11项任务全部完成** ✅

- **版本**: 1.52.8 → 1.52.9
- **构建产物**:
  - 🪟 Windows Setup: 142,134,029 bytes (~136 MB) ✅
  - 🪟 Windows Portable: 141,792,478 bytes (~135 MB) ✅
  - 🐧 Linux AppImage: 180,622,589 bytes (173 MB) ✅
  - 🐧 Linux Deb: 109,057,910 bytes (105 MB) ✅
  - 📱 Android APK: 81,477,086 bytes (78 MB) ✅
- **部署**: 所有5平台产物上传至生产 `/var/www/html/downloads/` ✅
- **元数据**: 8个 versions.json 全部同步一致 md5=`e78cd5ea94fc72d86c03ed2108aa0e9e` ✅
- **更新服务**: update-server(3001) ✅ | lingjing-update-server(3002) ✅ | cloud-server(8000) ✅ 均已重启
- **API验证**: 全部3个 `/api/latest` → `{"version":"1.52.9"}` ✅
- **下载验证**: 5个平台文件 HTTP 200 全部通过 ✅
- **Git**: 本地 `c87f84667` ✅ | 生产bare ✅ | 构建服务器 ✅ | GitHub ❌(连接失败，需手动重试)
