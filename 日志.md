## [2026-06-15 01:20] v1.73.72 — 修复 IIFE + 白屏 + 全平台部署

### 修复概览
1. **__safeRequireCodepilot IIFE 立即执行 Bug**: build-main.mjs 第391行 `})();` → `})`（移除立即执行括号）
2. **Portable 白屏**: frontend/dist 不存在 → 执行 `pnpm --filter @codepilot/renderer build` 构建前端（17 assets）→ 重新 build-main.mjs 同步 renderer

### 修改文件
- `desktop/electron/scripts/build-main.mjs` — Phase 0.6: `})();` → `})`（移除IIFE立即执行）

### 版本号升级 v1.73.71 → v1.73.72
- `desktop/core/package.json`
- `desktop/frontend/package.json`
- `desktop/electron/package.json`
- `desktop/electron/electron-builder.json`
- `electron/node_modules/@codepilot/core/package.json`

### 部署状态
| 平台 | 文件 | 状态 |
|------|------|------|
| Windows Setup | 灵境 Setup 1.73.72.exe | ✅ 已部署到生产 |
| Windows Portable | LingJing-Portable-1.73.72-win-x64.exe | ✅ 已部署到生产 |
| Linux AppImage | LingJing-1.73.72-linux-x86_64.AppImage | ✅ 构建机完成，已部署 |
| Linux deb | LingJing-1.73.72-linux-x86_64.deb | ⏳ fpm xz 压缩中（构建机关闭暂不可达） |

### 验证
- **更新端点**: `:3002` → `1.73.72` ✅ | `:8000` → `1.73.72` ✅
- **versions.json**: latest=1.73.72, 3个Windows/Linux AppImage文件 ✅
- **Linux deb**: 暂未在 versions.json 中出现

### 待办
1. **Linux deb 部署**: 构建机恢复后，检查 deb 文件 + SCP上传到生产 + 更新 versions.json
2. **Git 提交**: `build-main.mjs` 源文件已修复（`})();`→`})`），需 `git commit -a -m "fix: remove IIFE immediate execution in __safeRequireCodepilot Phase 0.6"` 并推送到 build-machine、production、ide-origin
3. **GitHub 推送**: 需要手动处理 token 权限

### 源文件修复确认 (2026-06-15 补充)
- ✅ `D:\lingjing-ide\desktop\electron\scripts\build-main.mjs` 第391行已从 `})();` 改为 `})`
### Git 提交状态 (2026-06-15 补充)
- HEAD: `d10ab7a1` — message: "fix: remove IIFE immediate execution..."
- 父提交: `e6f9851d` (v1.73.71 fix)
- COMMIT_EDITMSG 匹配 ✅
- 通过 multi_agent 执行（73s，报告成功）
- ⚠️ 未推送（shell不可用，无法执行 git push）
- 📄 创建 `finish-v17372-deploy.bat` — 一键收尾脚本（shell恢复后运行）
- 📄 创建 `v17372-status.md` — 部署状态文档

### 备注
- 当前会话 shell 工具完全不可用（所有命令失败），无法执行 SSH/Git/构建操作
- 关键信息已保存到记忆系统（ID: mqds92lxl841fp, mqds952qy9eoou, mqdsempgivadb8）
- 下次会话恢复后：
  1. `git push build-machine master && git push production master && git push ide-origin master`
  2. 检查构建机 deb + 上传到生产 + 更新 versions.json
  3. 更新 latest-linux.yml 到 1.73.72
  4. GitHub 推送（需 token）

### Shell 状态
- ❌ 完全不可用（所有命令均失败）
- 已保存到记忆系统 mqdsempgivadb8
- 结论：当前 IDE 环境的 shell 工具存在根本性故障，无法在本会话中完成操作

---

## [2026-06-15 后续] @codepilot/core 安全包装增强

### 修复概览
1. **main.ts tools:list handler**: 添加 try/catch 保护 `await import('@codepilot/core')`
2. **build-main.mjs Phase 0.6**: 扩展正则，同时匹配 `require()` 和 `await import()` 模式

### 修改文件
- `desktop/electron/src/main.ts` — tools:list handler 添加 try/catch
- `desktop/electron/scripts/build-main.mjs` — 新增 `codepilotImportDestrRe` 和 `codepilotImportSimpleRe` 正则

### 验证
- ✅ 构建成功（49个 @codepilot/core 引用全部安全包装）
- ✅ dist/main.js: 50 个 `__safeRequireCodepilot` 调用，0 个原始 `require("@codepilot")`，0 个原始 `await import("@codepilot")`

---

## [2026-06-15] v1.73.73 — 全平台部署完成

### 版本号升级 v1.73.71 → v1.73.73
- core/package.json: 1.73.73
- frontend/package.json: 1.73.73
- electron/package.json: 1.73.73
- electron-builder.json: output dir 1.73.73

### 部署状态
| 文件 | 状态 |
|------|------|
| 灵境 Setup 1.73.73.exe (185MB) | ✅ 上传至生产 |
| LingJing-Portable-1.73.73-win-x64.exe | ✅ 上传至生产 |
| versions.json (3路径同步) | ✅ latest=1.73.73 |
| update-server | ✅ pm2 restart |
| API /api/latest | ✅ 返回 1.73.73 |

### 源文件变更
- `desktop/electron/src/main.ts` — tools:list handler try/catch
- `desktop/electron/scripts/build-main.mjs` — Phase 0.6 await import正则匹配

### Git 提交
- 本地 Git commit: 8e75e4a (master)
- Git 推送: ✅ ide-origin/production/build-machine 均已 up-to-date

### 收尾 (2026-06-15 补充)
1. ✅ **latest.yml**: 更新至 v1.73.73，包含正确 sha512 和 size
2. ✅ **versions.json**: 修正文件信息（sha512/size），3路径同步完成
3. ✅ **API 验证**: `/api/latest` 返回 v1.73.73，文件信息正确
4. ✅ **Git 推送**: ide-origin/production/build-machine 均已最新

---

## [2026-06-15] v1.73.73 hotfix — loadPrompts 缺失导致 Provider 崩溃

### 根因
`@codepilot/core` 从未实现 `loadPrompts()` / `getPrompt()` / `MAIN_PROMPT` 这三个导出：
- `desktop/core/src/agent/prompts.ts` **文件不存在**
- `core/src/index.ts` 的导出被注释：`// export { loadPrompts, ... }`
- `agent-ipc.ts` 和 `quest-ipc.ts` 却导入了它们
- 运行时 `import_core4.loadPrompts === undefined` → `TypeError: not a function`

### 修复文件
- `desktop/core/src/agent/prompts.ts` — **新建**，实现 `loadPrompts()`（扫描 prompts 目录缓存 .md 文件）、`getPrompt()`、`MAIN_PROMPT`
- `desktop/core/src/index.ts` — 取消注释 `loadPrompts`, `getPrompt`, `MAIN_PROMPT`, `clearPromptCache` 导出
- `desktop/electron/scripts/build-main.mjs` — 末尾增加 Final sync 步骤，确保 `syncExternalNodeModules()` 覆盖后 electron node_modules 拥有最新的 core dist

### Git 提交
- Commit: `a824285` — 已推送至 ide-origin/production/build-machine
- 版本: 1.73.73（未升级版本号，patch 在下一版统一处理）

### 待办
- [ ] 重建 Windows 安装包（需触发 electron-builder）
- [ ] 重建 Linux 安装包（需构建机执行）
- [ ] 提示文件内容需后续完善（当前无 prompts/ 目录时可正常工作—回退到代码内联默认值）

---

## [2026-06-14] v1.73.73 — Windows/Linux 安装包构建与部署

### 构建概述
1. **Windows 安装包**: 本地 electron-builder 构建成功（release-v17374）
2. **Linux AppImage**: 构建机 (192.168.1.9) 构建成功
3. **Linux deb**: 手动 dpkg-deb 构建中（fpm 在构建机上卡住）

### 部署产物（生产服务器）
| 平台 | 文件 | 大小 | 状态 |
|------|------|------|------|
| Windows Setup | 灵境 Setup 1.73.73.exe | 186MB | ✅ |
| Windows Setup (blockmap) | 灵境 Setup 1.73.73.exe.blockmap | 198KB | ✅ |
| Windows Portable | LingJing-Portable-1.73.73-win-x64.exe | 186MB | ✅ |
| Linux AppImage | LingJing-1.73.73-linux-x86_64.AppImage | 192MB | ✅ |
| versions.json | latest=1.73.73 | 1.2KB | ✅ |

### 关键问题与修复
1. **build-main.mjs 符号链接问题**: 构建机上 @codepilot/core 是 symlink → electron-builder 报错"must be under"，修复：`rm symlink && cp -rL source dest`
2. **after-pack-hook asar CLI**: 构建机上 `npx @electron/asar` 不兼容，报 `sh: 1: asar: not found`。修复：`npx @electron/asar` → `npx --package @electron/asar asar`
3. **electron-builder 输出目录被占用**: Windows 上 release-v17373 被锁定，改为 release-v17374
4. **fpm 在构建机上卡住**: fpm 处理 575MB linux-unpacked 时超时/卡死，AppImage 替代

### 技术决策
- **跳过 deb 构建**: fpm 在构建机上卡死（可能 OOM 或被 SIGTERM），AppImage 已是完整的 Linux 分发格式
- **after-pack hook 失败非关键**: asarUnpack 配置已提取 @codepilot 到 unpacked/，运行时正常解析
- **构建机策略**: SCP 同步本地 dist 文件 + 修复符号链接 → 在构建机运行 electron-builder --linux


### 补充部署 (2026-06-14 22:42)
- **Linux deb**: LingJing-1.73.73-linux-x86_64.deb (189MB) ✅ 已上传至生产服务器
- **versions.json**: 已添加 deb 入口，全平台（win/linux AppImage/linux deb）完整
- **deb 构建方法**: `ar rcs` + `tar czf` 手动构建（dpkg-deb 对中文路径支持不佳，fpm 卡死）


### after-pack-hook 完整修复 (2026-06-14 22:45)
- **问题**: 3处 `npx @electron/asar` 调用在构建机上失败（`sh: 1: asar: not found`）
- **修复**: 全部改为 `npx --package @electron/asar asar` 语法
- **文件**: `desktop/electron/scripts/after-pack-hook.cjs` (extract/pack/list 三处)
- **Git**: `7360357` — 已推送至 build-machine/production/ide-origin
 


---

## [2026-06-14] v1.73.75 — __safeRequireCodepilot v3 自动修复（彻底解决 loadPrompts 崩溃）

### 根因
v1.73.73 虽然热修复了运行中的 `app.asar.unpacked/node_modules/@codepilot/core/dist`，但自动更新只替换 `app.asar`，不更新 `app.asar.unpacked`。旧版 unpacked 中：
- `dist/agent/` 目录不存在 → `loadPrompts is not a function`
- `index.js` 中 `loadPrompts`/`getPrompt`/`MAIN_PROMPT` 导出被注释

### 修复方案
1. **electron-builder.json**: 添加 `extraResources` → `codepilot-core-dist`（包含完整的 @codepilot/core/dist）
2. **build-main.mjs**: `__safeRequireCodepilot` v3 → 加载 `@codepilot/core` 失败时，自动从 `process.resourcesPath/codepilot-core-dist` 复制到 `app.asar.unpacked/node_modules/@codepilot/core/dist`，然后重新 require
3. **core/dist/agent/prompts.js**: 已实现完整 `loadPrompts()`/`getPrompt()`/`MAIN_PROMPT` 导出

### 修改文件
- `desktop/electron/electron-builder.json` — 添加 `codepilot-core-dist` extraResource
- `desktop/electron/scripts/build-main.mjs` — v3 自动修复（safe-require wrapper）
- `desktop/core/src/agent/prompts.ts` — 新建（已有）
- `desktop/core/src/index.ts` — 取消注释导出（已有）

### 构建产物 (v1.73.75)
| 平台 | 文件 | 大小 | 状态 |
|------|------|------|------|
| Windows Portable | LingJing-Portable-1.73.75-win-x64.exe | 186MB | ✅ 已部署到生产 |
| Windows Setup | 灵境 Setup 1.73.75.exe | — | ✅ 已上传 |
| Linux AppImage | LingJing-1.73.75-linux-x86_64.AppImage | 177MB | ✅ 已部署到生产 |
| Linux deb | LingJing-1.73.75-linux-x86_64.deb | 105MB | ✅ 已部署到生产 |

### 验证
- main.js 包含 __safeRequireCodepilot v3 wrapper
- v3 包含 loadPrompts 检查 + codepilot-core-dist 资源路径
- dist/agent/prompts.js 存在于构建产物中
- 生产服务器 /root/releases/ 包含全部 4 个文件 + latest-linux.yml

### 待办
- Git 提交 + 推送（build-main.mjs v3 / electron-builder.json extraResources）
- 更新生产服务器 versions.json
- 重启 update-server (pm2 restart)
- 验证自动更新流程
