From 8d46afe013e4fcabdede8e80e23f3475ffa8efa8 Mon Sep 17 00:00:00 2001
From: CodeArts AI Agent <codearts@zhejiangjinmo.com>
Date: Wed, 20 May 2026 09:47:53 +0800
Subject: [PATCH] docs: update for v1.44.6 release

---
 ACTIVE_TASK.md                | 59 +++++++++++++++++-------------
 "\346\227\245\345\277\227.md" | 67 +++++++++++++++++++++++++++++++++++
 2 files changed, 102 insertions(+), 24 deletions(-)

diff --git a/ACTIVE_TASK.md b/ACTIVE_TASK.md
index 3dec470b8..461e1dacd 100644
--- a/ACTIVE_TASK.md
+++ b/ACTIVE_TASK.md
@@ -1,35 +1,46 @@
 # 灵境 IDE — 任务跟踪
 
-## 当前状态：✅ 全部完成，系统健康
+## 当前状态：✅ v1.44.6 已部署
 
-### 最新版本：v1.44.5
-发布说明：修复 5 个 Bug（索引 100K 限制、云登录超时、loadPrompts 缺失、构建产物不完整、require ESM 崩溃）+ 构建配置优化
+### 最新版本：v1.44.6
+发布说明：Critical fix — ESM/CJS module mismatch (require is not defined)
 
 ### 已部署的平台包
 | 平台 | 文件 | 大小 | 状态 |
 |:-----|:-----|:----:|:----:|
-| 🪟 Windows Setup | LingJing-Setup-1.44.5-win-x64.exe | 98.6 MB | ✅ 生产环境 |
-| 🪟 Windows Portable | LingJing-Portable-1.44.5-win-x64.exe | ~98 MB | ✅ 生产环境 |
-| 🐧 Linux AppImage | LingJing-1.44.5-linux-x86_64.AppImage | 134 MB | ✅ 生产环境 |
-| 🐧 Linux deb | LingJing-1.44.5-linux-x86_64.deb | 108 MB | ✅ 生产环境 |
+| 🪟 Windows Setup | LingJing-Setup-1.44.6-win-x64.exe | 131 MB | ✅ 生产环境 |
+| 🪟 Windows Portable | LingJing-Portable-1.44.6-win-x64.exe | 130 MB | ✅ 生产环境 |
+| 🐧 Linux AppImage | LingJing-1.44.6-linux-x86_64.AppImage | 178 MB | ✅ 生产环境 |
+| 🐧 Linux deb | LingJing-1.44.6-linux-x86_64.deb | 108 MB | ✅ 生产环境 |
+
+### v1.44.6 修复内容
+
+**Bug**: `ReferenceError: require is not defined in ES module scope`
+用户在安装版灵境启动时遇到此错误，崩溃无法使用。
+
+**双根因分析：**
+
+1. **`packages/electron/package.json` 含 `"type": "module"`** 
+   - 这个文件在早期版本中被错误地替换成了 `@codepilot/core` 的 package.json 副本
+   - ASAR 根目录 package.json 声明了 ESM 模块类型
+   - Node.js/Electron 尝试将 CJS 格式的 `main.js` 作为 ESM 加载
+   - 所有 `require()` 调用因 `require is not defined` 而崩溃
+
+2. **3 个子路径保持 external**
+   - `checkpoint`、`rules`、`utils` 在 `build-main.cjs` 中被标记为 external
+   - 运行时 `require('@codepilot/core/checkpoint')` 加载 ESM 文件 → CJS/ESM 不兼容
+
+**修复方案：**
+- **Fix 1**: 恢复 `packages/electron/package.json` 的正确格式（`name: lingjing-ide`，无 `type:module`，正确依赖列表）
+- **Fix 2**: 从 `build-main.cjs` 的 external 列表中移除 checkpoint/rules/utils，全部 bundle 进 main.js
+- 验证：main.js 中 0 个 external `@codepilot/core` require，CheckpointManager 等已内联打包
 
 ### Git 三端同步
-- GitHub: `d8ff704e6` ✅（已推送）
-- Tag `v1.44.5`: `d8ff704e6` ✅
-- 本地: `d8ff704e6` ✅
-
-### v1.44.5 修复清单
-1. ✅ Bug 2: 索引 100K 限制 → 500K，支持 `DEFAULT_MAX_INDEX_FILES` 配置化
-2. ✅ Bug 3: 云登录 Failed to fetch → 添加 15s AbortController 超时 + 中文友好提示
-3. ✅ Bug 5: loadPrompts 导出缺失 → dist/index.js + src/index.ts 添加 13 个缺失导出
-4. ✅ 构建修复: 从 external 移除 `@codepilot/core` + resolveCorePlugin 避免 ESM require 崩溃
-5. ✅ Windows/Linux 全平台构建部署
-6. ✅ 3 个积压 commit 推送至 GitHub
+- GitHub: `cab3109` ✅（通过服务器中转推送）
+- Tag `v1.44.6`: `cab3109` ✅
+- 服务器: `cab3109` ✅
 
 ### 服务健康
-- PM2: 4/4 online 🟢（cloud-server, scms-server, scms-web, update-server）
-- API /health: `{"status":"ok"}` ✅
-- API /latest: v1.44.5 ✅（4个平台文件全）
-- 磁盘: 28%（103G 可用, 148G 总容量）✅
-
-_如需新任务请直接提出需求_
+- PM2: 4/4 online 🟢
+- API /api/latest: v1.44.6 (4 平台文件) ✅
+- 磁盘: ~29% ✅
diff --git "a/\346\227\245\345\277\227.md" "b/\346\227\245\345\277\227.md"
index 08458f6c8..20f5d2605 100644
--- "a/\346\227\245\345\277\227.md"
+++ "b/\346\227\245\345\277\227.md"
@@ -2299,3 +2299,70 @@ packages/electron/src/preload.ts 有 listConnections 定义，但编译产物 di
   - Windows Setup + Portable ✅
   - Linux AppImage + deb ✅
 
+---
+
+## Phase: v1.44.6 Critical Fix — ESM/CJS module mismatch (2026-05-20)
+
+### Bug
+用户报告安装版灵境启动时出现 `ReferenceError: require is not defined in ES module scope`。
+
+### 根因分析
+
+**根因 1：`packages/electron/package.json` 含 `"type": "module"`**
+- 该文件在 v1.44.x 版本中被错误替换为 `@codepilot/core` 的 package.json 副本
+- 导致 ASAR 根目录 package.json 声明了 ESM 模块类型
+- Node.js/Electron 尝试将 CJS 格式（esbuild 构建为 CJS）的 `main.js` 作为 ESM 加载
+- 所有 `require()` 调用因 `require` 在 ESM 作用域未定义而崩溃
+
+**根因 2：3 个子路径保持 external**
+- `build-main.cjs` 将 `checkpoint`、`rules`、`utils` 三个子路径标记为 external
+- 运行时 `require('@codepilot/core/checkpoint')` 尝试加载 ESM 文件 → CJS/ESM 不兼容
+
+### 修复
+
+| 文件 | 变更 |
+|:-----|:------|
+| `packages/electron/package.json` | 恢复正确格式：`name: lingjing-ide`、移除 `type:module`、移除 exports map、恢复正确依赖（electron/electron-builder/esbuild 等 24个运行时依赖 + 4个开发依赖） |
+| `packages/electron/scripts/build-main.cjs` | 移除 `EXTERNAL_CORE_SUBPATHS` 列表（checkpoint/rules/utils），全部通过 `resolveCorePlugin` 映射后 bundle 进 main.js |
+| `packages/electron/electron-builder.json` | output: `release-v1445` → `release-v1446` |
+| `packages/core/package.json` | version: 1.44.5 → 1.44.6 |
+
+### 验证结果
+
+| 检查项 | 结果 |
+|:-------|:------|
+| main.js 含 CheckpointManager (5次) | ✅ |
+| main.js 含 registerCheckpointIPC (2次) | ✅ |
+| main.js 含 RollbackExecutor (5次) | ✅ |
+| main.js 0 个 external @codepilot require | ✅ |
+| main.js 为 CJS 格式 (require + module.exports) | ✅ |
+| core 测试 434/434 通过 | ✅ |
+| 渲染器构建成功 | ✅ |
+| GitHub 推送 (cab3109) + tag v1.44.6 | ✅ |
+
+### 构建产物
+
+| 平台 | 文件 | 大小 | 状态 |
+|:-----|:-----|:----:|:----:|
+| 🪟 Windows Setup | LingJing-Setup-1.44.6-win-x64.exe | 131 MB | ✅ 已部署 |
+| 🪟 Windows Portable | LingJing-Portable-1.44.6-win-x64.exe | 130 MB | ✅ 已部署 |
+| 🐧 Linux AppImage | LingJing-1.44.6-linux-x86_64.AppImage | 178 MB | ✅ 已部署 |
+| 🐧 Linux deb | LingJing-1.44.6-linux-x86_64.deb | 108 MB | ✅ 已部署 |
+
+### API 验证
+- `/api/latest` → v1.44.6 (4 平台文件全) ✅
+
+### 涉及文件
+- `packages/core/package.json` — 1.44.5 → 1.44.6
+- `packages/electron/package.json` — 完整恢复（移除 type:module，恢复正确名称和依赖）
+- `packages/electron/electron-builder.json` — output → release-v1446
+- `packages/electron/scripts/build-main.cjs` — 移除 3 个 external 子路径
+
+### 部署步骤
+1. 本地构建 Windows Setup + Portable ✅
+2. 上传到生产服务器 ✅
+3. 同步代码到服务器并推送 GitHub（GFW 阻断，通过服务器中转推送）✅
+4. 服务器构建 Linux AppImage + deb ✅
+5. 更新 latest.yml / latest-linux.yml / versions.json ✅
+6. 重启 cloud-server 清除缓存 ✅
+
-- 
2.53.0.windows.1

