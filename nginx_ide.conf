# ⚠️ 此文件已废弃 - 仅作历史参考
#
# 生产环境的 nginx 配置位于生产服务器(120.55.5.220):
#   /etc/nginx/sites-enabled/lingjing.zhejiangjinmo.com.conf
#   （企业API通过主域名 /api/v1/ 路径代理，不再使用独立子域名）
#
# 生产配置使用 Node.js SFTP 直接管理，不由 git 维护。
# 如需查看当前生效配置，SSH 到生产服务器或使用 SFTP 读取。
#
# 关键差异:
# - 生产配置有完整的 CDN 回源路由(/downloads/, /min-version.txt, /api/rollout/)
# - 生产配置有 mobile app 版本检查 API 路由(/api/v1/app/)
# - 生产 HTTP(80)和 HTTPS(443)块必须同步路由
