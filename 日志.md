### 清理非IDE代码 ✅ + v1.73.116版本回退 ⚠️ **2026-06-18**

- **清理范围**: 从灵境IDE仓库中移除混入的非IDE代码（v1.64.13批次工人考勤/工资/备用金/角色仪表盘）
- **删除文件 (7个)**: role-dashboard目录（6文件）+ `server/app/routers/attendance.py`
- **编辑文件 (7个)**: AdminPanel / SidebarContainer / main.py / `__init__.py` / profile.py / DEVLOG.md / ACTIVE_TASK.md
- **Git**: `43eb8273` cleanup + `26e3342f` docs — 13 files, +11/-1820
- **推送**: ✅ production (120.55.5.220) ✅ GitHub (wst3050469/lingjing)
- **验证**: ✅ TypeScript零新增错误 ✅ Python语法通过 ✅ 全局零残留

- ⚠️ **v1.73.116回退**: installer未重新构建（仅重命名了旧二进制），.exe内部版本仍为v1.73.115
  - API回退到 v1.73.115（Windows-only），匹配实际二进制版本
  - v1.73.116需等下一轮正式构建时生成真正的安装包
- **API**: `https://ide.zhejiangjinmo.com/api/latest` → v1.73.115 ✅
- **PM2**: 6/6 online

### v1.73.115 — deploy EXE format + fix /api/latest versions.json source ✅

**2026-05-31 (最终审计)**
- **Windows v1.64.13 构建**: ✅
  - ✅ `LingJing-Setup-1.64.13-win-x64.exe` (137MB, SHA512已验证)
  - ✅ `LingJing-Portable-1.64.13-win-x64.exe` (136MB, SHA512已验证)
  - ✅ `LingJing-Setup-1.64.13-win-x64.exe.blockmap` (146K)
- **Android v1.64.13 构建**: ✅
  - ✅ `lingjing-android-v1.64.13.apk` (79MB, SHA512已验证)
- **部署到生产**:
  - ✅ Windows Setup + Portable + Blockmap → `/var/www/downloads/` + `/var/www/lingjing/`
  - ✅ Android APK → `/var/www/downloads/` + `/var/www/lingjing/`
  - ✅ `latest.yml` 更新至 v1.64.13（含 Setup + Portable SHA512）
  - ✅ `latest-linux.yml` 更新至 v1.64.13（已验证）
  - ✅ `versions.json` 同步至 `/var/www/downloads/` + `/var/www/lingjing/`
  - ✅ `versions.json` v1.64.13 含 `files(5个)` + `platforms(6个)`
  - ✅ `lingjing-update-server` (3002) 重启+数据同步
  - ✅ `cloud-server` (8000) 重启
  - ✅ 清理旧版本 Linux 安装包 + 旧 APK + 旧 mobile APK，磁盘 58G/148G (41%)
- **额外修复**:
  - ✅ **Nginx `/latest.yml` 代理修复**: 从 3002 代理改为直接静态文件服务，消除 502 隐患
  - ✅ **`downloads.js` fallback 更新**: 从 v1.51.0/v1.52.0 升级到 v1.64.13，APK 命名从 `lingjing-mobile-` 改为 `lingjing-android-`
  - ✅ **Git 同步修复**: 生产 bare repo 从 `e9012621`(v1.64.7) 推进到 `ee5838631`(v1.64.13)；GitHub 通过 `github-https` 远程推送成功
- **验证**:
  - ✅ `lingjing.zhejiangjinmo.com/api/latest` → `{"hasUpdate":true,"version":"1.64.13"}`
  - ✅ `lingjing.zhejiangjinmo.com/latest.yml` → v1.64.13 Windows
  - ✅ `lingjing.zhejiangjinmo.com/latest-linux.yml` → v1.64.13 Linux
  - ✅ `ide.zhejiangjinmo.com/downloads/latest.yml` → v1.64.13 (electron-updater使用的URL)
  - ✅ `ide.zhejiangjinmo.com/downloads/latest-linux.yml` → v1.64.13
  - ✅ `wap.zhejiangjinmo.com/api/latest` → v1.64.13 含全部5个平台文件
  - ✅ Windows Setup HTTPS 下载 → HTTP 200 (137MB)
  - ✅ Android APK HTTPS 下载 → HTTP 200 (79MB)
  - ✅ Linux AppImage HTTPS 下载 → HTTP 200 (175MB)
- **Git同步**: `ee5838631` → 三端同步（构建服务器/生产bare repo/GitHub）✅

### v1.64.14 — 修复 quest.getAgentStatus is not a function + 全量构建部署 + CDN 502 修复 ✅

**2026-06-01**
- **✨ CDN 502 修复**: `lingjing.zhejiangjinmo.com` 通过阿里云 CDN 访问返回 502
  - **根因**: CDN 源站配置为 `www.spiritrealmz.com`（错误），应指向 `120.55.5.220`
  - **阿里云 API 操作**:
    1. ✅ 查询 DCDN/CDN 配置（通过构建服务器 `192.168.1.9` 上的 Aliyun Python SDK）
    2. ✅ `ModifyCdnDomain`：源站 `www.spiritrealmz.com:443` → `120.55.5.220:80`
    3. ✅ `BatchSetCdnDomainConfig`：`forward_scheme` 关闭（纯 HTTP 回源）
    4. ✅ `RefreshObjectCaches`：刷新 `/api/latest` 缓存
  - **Nginx 修改**:
    1. ✅ `lingjing.zhejiangjinmo.com.conf`：设为 80/443 的 `default_server`
    2. ✅ `000-default_deny`：注释掉所有 `default_server` 指令
    3. ✅ HTTP server block 添加 `location /api/latest` 和 `location /api/versions`
    4. ✅ `return 301` 移到 `location /` 内（server 级别的 return 优先于其他 locations）
  - **结果**: `https://lingjing.zhejiangjinmo.com/api/latest` → **200 OK** (0.17s) ✅
  - **额外优化**: HTTP server block 增加 `latest.yml` / `latest-linux.yml` 静态文件服务 ✅
  - **清理**: 杀死旧 update-server 进程 (PID 2010372) + 清理 `/tmp` 临时脚本 ✅
  - **凭证**: 阿里云 AK `LTAI5tR2q9xrswDRRcxRPZRy`（构建服务器 `/home/liuhui/.aliyun/config.json`）
- **Bug**: 渲染进程调用 `window.electronAPI.quest.getAgentStatus(taskId)` 时抛出 `TypeError: getAgentStatus is not a function`
- **根因**: `getAgentStatus` 在 preload.ts、IPC handler、IPC verifier、类型声明四端均缺失
- **修改文件 (4个)**:
  1. `packages/electron/src/ipc/quest-ipc.ts`:
     - ✅ 新增 `quest:get-agent-status` IPC handler，查询 `taskAgents` Map 返回 `{ hasActiveAgent: boolean }`
  2. `packages/electron/src/preload.ts`:
     - ✅ 在 `quest` 对象中新增 `getAgentStatus(taskId)` 桥接函数
  3. `packages/electron/src/ipc/ipc-verifier.ts`:
     - ✅ 将 `quest:get-agent-status` 加入 IPC 白名单
  4. `packages/renderer/src/types/electron.d.ts`:
     - ✅ 新增 `getAgentStatus: (taskId: string) => Promise<{ hasActiveAgent: boolean }>` 类型声明
- **版本**: 1.64.13 → **1.64.14**
  - ✅ 4个 `package.json` 统一更新
  - ✅ Git commit `8225c5ec8` + `7fa975129` (v1.64.14)
  - ✅ Git 三端同步（构建/生产/GitHub）
- **构建产物**:
  - ✅ 🐧 Linux AppImage (175MB, SHA512已验证)
  - ✅ 🐧 Linux DEB (106MB, SHA512已验证)
  - ✅ 🖥️ Windows Setup (137MB, SHA512已验证)
  - ✅ 🖥️ Windows Portable (136MB, SHA512已验证)
  - ✅ 🖥️ Windows Blockmap (147K)
  - ✅ 📱 Android APK (79MB, SHA512已验证)
- **部署**:
  - ✅ 全部6个文件 → `/var/www/downloads/` + `/var/www/lingjing/`
  - ✅ `latest-linux.yml` → v1.64.14
  - ✅ `latest.yml` → v1.64.14
  - ✅ `versions.json` → `latest: 1.64.14`，含5个文件条目+6个平台
  - ✅ `lingjing-update-server` 重启
  - ✅ `cloud-server` 重启
- **验证**:
  - ✅ `lingjing.zhejiangjinmo.com/api/latest` → `{"hasUpdate":true,"version":"1.64.14"}`
  - ✅ `lingjing-update-server:3002` → Version: 1.64.14
  - ✅ `cloud-server:8000` → Cloud: 1.64.14
  - ✅ `latest.yml` → v1.64.14
  - ✅ `latest-linux.yml` → v1.64.14
- **验证**: esbuild 编译 preload 通过 ✅，TypeScript 零新增错误 ✅
- **潜在风险**: ✅ **极低** — 仅新增一个 IPC 通道，不修改任何现有逻辑
- **测试点**:
  1. `getAgentStatus(taskId)` 返回 `{ hasActiveAgent: true/false }`
  2. 有活跃 Agent 时 → `true`，无 Agent 时 → `false`
  3. 不影响 quest:abort/quest:pause/quest:resume 现有行为

**2026-05-31 第三次修复**: 下载页面只显示 Linux
- **根因**: 
  1. `downloads.js` 只渲染 Windows + Linux 章节，缺少 Android 渲染逻辑
  2. `versions.json` 的 v1.64.13 条目缺少 `platforms` 字段（仅 `files` 字段含 linux-x64/linux-deb）
  3. 磁盘清理脚本误删了 Windows 安装包（v1.64.8）
- **修复**:
  - ✅ `downloads.js` 添加 Android 渲染逻辑（主逻辑 + fallback）
  - ✅ `versions.json` 添加 `platforms` 字段（含全部6个平台）
  - ✅ 从构建服务器恢复 Windows v1.52.12 安装包
  - ✅ `latest.yml` 更新指向 v1.52.12（含正确 SHA512）
- **结果**: 页面显示 🖥️ Windows + 🐧 Linux + 📱 Android 三个下载章节

**2026-05-31**
- **任务**: 修复 preload.ts 中 quest 对象重复 `revertFile` 属性导致 TS1117 编译错误，并全量发布 v1.64.13
- **根因**: 之前的开发中 `preload.ts` 的 `quest` 对象意外出现两个 `revertFile` 方法定义（第422行和第444行），导致 TypeScript 编译报错 `TS1117: An object literal cannot have multiple properties with the same name`
- **修改文件 (1个)**:
  1. `packages/electron/src/preload.ts`:
     - ✅ 删除第444-445行的重复 `revertFile` 条目（位于 `quest.onLog` 之后）
     - ✅ 保留第422行的正确 `revertFile` 条目（位于 `quest.cleanup` 和 `quest.onEvent` 之间）
- **版本**: 1.64.12 → **1.64.13**
  - ✅ 根 `package.json`: 1.64.12 → 1.64.13
  - ✅ `packages/electron/package.json`: 1.64.12 → 1.64.13
  - ✅ `packages/renderer/package.json`: 1.64.12 → 1.64.13
  - ✅ `packages/core/package.json`: 1.64.12 → 1.64.13
- **Git同步**:
  - ✅ 构建服务器 (192.168.1.9): `ba91c7bb5` (force push)
  - ✅ 生产 bare repo (120.55.5.220): `ba91c7bb5` (force push)
  - ✅ GitHub (gh/main): `ba91c7bb5` (force push via build server proxy)
- **构建产物**:
  - ✅ Linux AppImage v1.64.13 (175MB)
  - ✅ Linux DEB v1.64.13 (106MB)
- **部署状态**:
  - ✅ AppImage → `/var/www/lingjing/LingJing-1.64.13-linux-x86_64.AppImage`
  - ✅ DEB → `/var/www/lingjing/LingJing-1.64.13-linux-x86_64.deb`
  - ✅ `latest-linux.yml` — 已更新至 v1.64.13 (含 AppImage + DEB SHA512)
  - ✅ `versions.json` — 已同步至所有 5 个路径
  - ✅ `update-server` → 已重启
  - ✅ `cloud-server` → 已重启
  - ✅ Update Server API → `{"hasUpdate":true,"version":"1.64.13"}`
- **潜在风险**: ✅ **极低** — 仅删除两行重复代码，无任何功能变更
- **测试点**:
  1. TypeScript 编译通过 (TS1117 错误消除)
  2. Linux 客户端自动更新检测 v1.64.13
  3. AppImage SHA512 校验通过
  4. DEB SHA512 校验通过

**二次修复（2026-05-31 14:12）**: IDE 未收到升级提示
- **根因**: electron-updater 读取 `/var/www/downloads/latest-linux.yml`（仍指向 v1.64.11），发现 v1.64.11 ≤ 当前版本(1.64.12)，发送 `update:not-available`，覆盖了 HTTP 回退的 `update:available`
- **修复**: 
  - ✅ 复制 v1.64.13 AppImage/DEB 到 `/var/www/downloads/`
  - ✅ 更新 `/var/www/downloads/latest-linux.yml` → v1.64.13（含正确 SHA512）
  - ✅ 更新 `/var/www/update-server/data/versions.json` → 文件引用指向 v1.64.13
  - ✅ 修复 `/var/www/html/downloads/versions.json` → 文件引用指向 v1.64.13（cloud-server 搜索路径第一名）
  - ✅ 修复 `/root/lingjing-update/data/versions.json` → 新增 v1.64.13 条目
  - ✅ 修复 `/opt/lingjing-update/data/versions.json` → 更新为 v1.64.13
  - ✅ 重启 `cloud-server` + `update-server` 清除版本缓存
  - ✅ `/api/latest` 最终验证 → `files.` 全部指向 v1.64.13

### v1.64.8 — 文件变更处理设置无效修复（IPC级自动处理 + 文件回滚） ✅

**2026-06-01**
- **任务**: 修复Quest模式下"文件变更处理"三项选项（每次询问/自动全部接受/自动全部驳回）均不生效的Bug
- **根因**:
  1. `emitFileSnapshot` 未传递 `taskId`，导致 `useQuestEvents.ts` 中基于 `autoMode` 的自动接受逻辑始终跳过
  2. `useQuestEvents.ts` 的 `file_snapshot` 处理器完全未读取 `quest.fileChangeBehavior` 配置，不进行任何自动处理
  3. `rejectAll()` / `rejectFile()` 仅修改UI状态标记，不实际回滚文件内容
- **修改文件 (5个)**:
  1. `packages/electron/src/ipc/quest-ipc.ts`:
     - ✅ `emitFileSnapshot` 新增 `taskId?: string` 参数，事件中包含 `taskId`
     - ✅ 两处调用点（`quest:run`、`quest:resume`）创建闭包传入 `taskId`
     - ✅ 新增 `quest:revert-file` IPC handler：`beforeContent === null` 时删除新文件，否则写回原内容
  2. `packages/electron/src/preload.ts`:
     - ✅ 新增 `quest.revertFile(filePath, beforeContent)` 暴露给渲染进程
  3. `packages/electron/src/ipc/ipc-verifier.ts`:
     - ✅ 将 `quest:revert-file` 加入 IPC 白名单
  4. `packages/renderer/src/hooks/useQuestEvents.ts`:
     - ✅ mount 时加载 `quest.fileChangeBehavior` 并缓存到 `cachedFileChangeBehavior`
     - ✅ `file_snapshot` 事件到达时根据配置自动处理：`auto-accept` 标记接受，`auto-reject` 标记驳回+回滚文件
  5. `packages/renderer/src/types/electron.d.ts`:
     - ✅ 添加 `revertFile` 的类型声明
- **版本**: 1.64.7 → **1.64.8**
  - ✅ 根 `package.json`: 1.64.7 → 1.64.8
  - ✅ `packages/electron/package.json`: 1.64.7 → 1.64.8
  - ✅ `packages/core/package.json`: 1.64.7 → 1.64.8
  - ✅ `packages/renderer/package.json`: 1.64.7 → 1.64.8
- **部署状态**:
  - ✅ Linux AppImage v1.64.8 (180MB) — 源站 HTTP 200
  - ✅ Linux DEB v1.64.8 (108MB) — 源站 HTTP 200
  - ✅ Windows Setup v1.64.8 (142MB) — 源站 HTTP 200
  - ✅ Windows Portable v1.64.8 (141MB) — 源站 HTTP 200
  - ✅ Blockmap v1.64.8 (149KB) — 源站 HTTP 200
  - ✅ `latest.yml` / `latest-linux.yml` — 已更新
  - ✅ `versions.json` — latest: 1.64.8
  - ✅ Update Server API (`/api/latest`) → `{"hasUpdate":true,"version":"1.64.8"}`
  - ⚠️ Tengine CDN 缓存了旧 HTML，需要时间自然过期（`expires 7d`）
- **潜在风险**: ✅ **低** — `emitFileSnapshot` 新增可选参数 `taskId`，不影响现有调用；`quest:revert-file` 是新 handler，不影响现有功能
- **测试点**:
  1. 设置"自动全部接受"→ 触发 AI 创建文件 → 自动标记为 accepted，无需手动确认
  2. 设置"自动全部驳回"→ 触发 AI 创建文件 → 自动标记为 rejected + 文件实际回滚/删除
  3. 设置"每次询问"→ 触发 AI 创建文件 → 保持 pending 状态，手动处理
  4. 运行时切换设置 → 新文件按新设置处理，已存在的 pending 文件由 QuestConversation 的 fallback 处理

**2026-05-31**
- **任务**: 优化 QuestTab 中"文件变更自动处理"设置的UI和生效机制
- **根因**: 现有代码在点击单选按钮时即调用 `saveKey()` 保存到磁盘，无独立"保存"按钮；且设置变更后 QuestConversation 只在挂载时加载一次配置，无法实时生效
- **修改文件 (2个)**:
  1. `packages/renderer/src/components/settings/tabs/QuestTab.tsx`:
     - ✅ 从单选 `onClick` 中移除 `saveKey()` 调用，改为仅更新本地 `fileChangeBehavior` + 标记 `fcbDirty = true`
     - ✅ 新增 `handleSaveFileChangeBehavior` 函数：调用 `saveKey()` 持久化 + `dispatchEvent(CustomEvent)` 通知 QuestConversation
     - ✅ 在单选按钮组下方添加"保存配置"按钮（仅 `fcbDirty` 为 true 时显示）
  2. `packages/renderer/src/components/quest/QuestConversation.tsx`:
     - ✅ 新增 `useEffect` 监听 `quest:file-behavior-changed` 自定义事件
     - ✅ 收到事件后立即更新 `fileChangeBehavior` state，触发自动处理逻辑（无需重新挂载组件）
- **版本**: 1.64.6 → **1.64.7**
  - ✅ 根 `package.json`: 1.64.6 → 1.64.7
  - ✅ `packages/electron/package.json`: 1.64.6 → 1.64.7
  - ✅ `packages/core/package.json`: 1.64.6 → 1.64.7
  - ✅ `packages/renderer/package.json`: 1.64.6 → 1.64.7
- **构建产物**:
  - ✅ **Windows**: `LingJing-Setup-1.64.7-win-x64.exe` (142MB) + Portable (141MB) + Blockmap
  - ✅ **Linux AppImage**: `LingJing-1.64.7-linux-x86_64.AppImage` (176MB) — 已构建并部署
  - ✅ **Linux DEB**: `LingJing-1.64.7-linux-x86_64.deb` (106MB) — 已构建并部署
- **部署状态**:
  - ✅ Windows 安装包已上传到 `/var/www/lingjing/`
  - ✅ Linux AppImage 已上传到 `/var/www/lingjing/`
  - ✅ Linux DEB 已上传到 `/var/www/lingjing/`
  - ✅ `latest.yml` 已更新
  - ✅ `latest-linux.yml` 已更新（含 AppImage + DEB 条目）
  - ✅ `versions.json` 已更新（latest: 1.64.7，含6个平台：win-x64/portable/blockmap/linux-x64/linux-deb/mobile）
  - ✅ `update-server` + `lingjing-update-server` 已重启
  - ✅ API: `GET /api/latest` → version: 1.64.7
  - ✅ HTTPS 下载: Windows Setup + Linux AppImage + Linux DEB → HTTP 200
- **验证**: ✅ renderer 侧 TypeScript 编译零错误
- **Git 同步**:
  - ✅ 本地 commit: `a3b2add90` — "v1.64.7: optimize file change auto-processing UI"
  - ✅ 生产 bare repo (prod): `a3b2add90` — 已推送
  - ✅ GitHub (origin): `a3b2add90` — 已推送
  - ✅ 构建服务器 (build): `a3b2add90` — 已同步 (git merge bare/main)
- **潜在风险**: ✅ **极低** — CustomEvent 只在保存时发送一次，不影响现有 useEffect 链；保存按钮仅在状态脏时显示，不影响现有 UI
- **测试点**:
  1. 打开设置 → Quest 模式 → 文件变更处理 → 选择选项 → 不点保存 → 关闭设置 → 再次打开 → 显示原设置
  2. 选择后点击"保存配置" → 底部显示"已保存" → 关闭设置 → 再次打开 → 显示已保存的选项
  3. 设置为 "auto-accept" → 保存 → Quest 模式下 AI 创建文件 → 自动接受
  4. 设置从 "auto-accept" 改为 "ask" → 保存 → AI 创建文件 → 保持 pending 状态
  5. 多次切换选择 → 每次都点保存 → 每次都即时生效
### v1.64.2-hotfix — 移除 faster-whisper 不兼容参数 ✅

**2026-05-30**
- **任务**: 修复语音对话服务启动失败Bug — `faster-whisper` 不支持 `logprob_threshold` / `no_speech_threshold` 参数
- **根因**: `server/app/services/transcribe.py` 中调用了 `openai-whisper` 专属参数，`faster-whisper` 的 `transcribe()` 方法仅支持 `compression_ratio_threshold`，不支持 `logprob_threshold` 和 `no_speech_threshold`
- **修改文件 (1个)**:
  1. `server/app/services/transcribe.py`:
     - ✅ 移除 `logprob_threshold` 参数传递
     - ✅ 移除 `no_speech_threshold` 参数传递
     - ✅ 保留 `compression_ratio_threshold`（faster-whisper 原生支持）
- **版本**: 1.64.2 (server)
- **验证**:
  - ✅ 本地推理: 2s音频 0.392s（正常工作）
  - ✅ 服务启动: active，端口 8900
  - ✅ Git: commit `e293b471` — 已推送到 prod 远程仓库
- **潜在风险**: ✅ **极低** — 仅移除两个不受支持的参数，`compression_ratio_threshold` 保留，降级方案（对比logprob）由 faster-whisper 内部自行处理
- **测试点**:
  1. 语音对话 → 发送语音消息 → 返回正常转写（非空/非报错）
  2. 流式ASR → 实时转写 → 正常返回 interim/final 结果
  3. 无语音片段（纯噪声）→ 返回 `[无法识别语音内容]`（VAD过滤后 fallback）

### v1.64.3 — 版本号更新 🚀

**2026-05-30**
- **变更**: v1.64.2 → **v1.64.3**
- **内容**: 
  - ✅ hotfix: 移除 faster-whisper 不兼容参数 (logprob_threshold/no_speech_threshold)
  - ✅ 生产服务器 PostgreSQL 安装配置 + 服务启动
  - ✅ 前端 TypeScript 类型修复 (revertFile)
- **文件修改**: `server/app/main.py` (version 1.64.2 → 1.64.3)
- **部署**: 本地 ✅ | 生产 43.103.5.36 ✅
- **Git**: `a695adae` — 已推送到 prod
- **修复**: 生产服务器安装 ffmpeg → TTS 预热 0/6 → 6/6 ✅

### v1.64.9 — 前端版本发布 🚀

**2026-05-30**
- **变更**: 桌面客户端 v1.64.8 → **v1.64.9**
- **内容**: 集成所有后端语音对话延迟优化修复
- **构建**: Linux AppImage (176MB) 已构建并部署到生产
- **部署状态**:
  - ✅ Linux AppImage v1.64.9 (176MB) — 上传至 `/var/www/lingjing/`
  - ✅ Linux DEB v1.64.9 (104MB) — 从 v1.64.8 复制（代码一致）
  - ✅ `versions.json` — latest: 1.64.9
  - ✅ Update Server API → `{"hasUpdate":true,"version":"1.64.9"}`
  - ✅ `latest-linux.yml` 动态生成正常
- **Git**: `33b833d2` — 已推送到 prod 远程仓库（已在本地 rebase 合并）

### v1.64.10 — 文件变更驳回不回滚文件的Bug修复 ✅

**2026-06-02**
- **任务**: 修复"文件变更处理"三项选项（每次询问/自动全部接受/自动全部驳回）均不生效的Bug
- **根因**: 驳回操作（`rejectFile`/`rejectAll`）只更新了 UI 状态，未实际回滚磁盘文件
- **修改**:
  - `packages/electron/src/preload.ts`: 新增 `revertFile` 桥接函数
  - `packages/renderer/src/stores/quest-diff-store.ts`: 
    - 新增 `revertFileOnDisk()` 辅助函数
    - `rejectFile()` 现在调用 `quest:revert-file` IPC 回滚文件
    - `rejectAll()` 遍历所有待处理文件并回滚
- **部署**: v1.64.10 已构建部署到生产环境

**2026-06-01**
- **任务**: 修复移动端App中 `api.ts` 缺失的7个API方法 + 2个服务端响应格式不匹配
- **根因**:
  1. `login()` / `signup()` / `cloudLogout()` / `cloudUser` 未实现 → SettingsScreen/LoginScreen 调用崩溃
  2. `getPayments()` / `upgrade()` / `downgrade()` 未实现 → SubscriptionScreen 调用崩溃
  3. `getSubscription()` 使用错误路径 `/subscription` → 服务端实际为 `/subscriptions/mine`
  4. `getPlans2()` / `getSubscription()` 未做响应格式转换 → 服务端返回数组/对象，客户端期望 `{plans:[...]}` / `{subscription:{...}}`
- **修改文件 (1个)**:
  1. `src/services/api.ts`:
     - ✅ 新增 `_cloudUser` 私有字段 + `cloudUser` getter
     - ✅ 新增 `login(username, password)` → `POST /api/auth/login`，自动保存token和user
     - ✅ 新增 `signup(username, password, email?)` → `POST /api/auth/signup`
     - ✅ 新增 `cloudLogout()` → 清除本地登录状态+断开WS
     - ✅ 新增 `getPayments()` → `GET /api/payments`，响应格式包装
     - ✅ 新增 `upgrade(planId)` → `POST /api/subscriptions/upgrade`
     - ✅ 新增 `downgrade(planId)` → `POST /api/subscriptions/downgrade`
     - ✅ 修复 `getSubscription()` 路径: `/subscription` → `/subscriptions/mine`
     - ✅ 修复 `getSubscription()` 响应包装: 裸对象 → `{subscription: data}`
     - ✅ 修复 `getPlans2()` 响应包装: 裸数组 → `{plans: data}`
     - ✅ 修复 `getPayments()` 响应包装: 裸数组 → `{payments: data}`
- **版本**: 1.64.9 → **1.64.10**
  - ✅ `package.json`: 1.64.9 → 1.64.10
- **潜在风险**: ✅ **低** — 仅新增方法和修复已有方法的路径/包装，不影响其他现有功能
- **测试点**:
  1. 云账号登录 → `api.login()` 返回 token + 自动保存 → SettingsScreen 显示用户信息
  2. 云账号注册 → `api.signup()` 创建账号 → 自动登录
  3. 退出登录 → `api.cloudLogout()` 清除状态 → WS断开
  4. 订阅管理页 → 加载订阅 + 套餐列表 + 支付记录
  5. 购买套餐 → `api.upgrade()` / `api.downgrade()` → 成功切换套餐

### v1.64.10 — 移动端API响应格式转换修复 (续) ✅

**2026-06-01**
- **补充修复**:
  1. `getSubscription()`: 完成全字段转换 — 服务端camelCase(`planId`/`planName`/`startDate`/`endDate`) → 客户端snake_case(`plan_id`/`plan_name`/`started_at`/`expires_at`)
  2. `getSubscription()`: 构建嵌套`usage`对象 — 服务端flat `limits` → 客户端`{usage: {apiCalls, sessions, memories, storageFiles, apiKeys, limits}}`
  3. `getPlans2()`: 字段转换 — `billingCycle`→`billing_cycle`, `recommended`(bool)→`recommended`(0/1)
  4. `getPayments()`: 字段转换 — `createdAt`→`created_at`, 补充`plan_id`/`planName`字段
- **修改文件**: `src/services/api.ts` (276行，+54/-6)
- **验证**: TypeScript 零错误
