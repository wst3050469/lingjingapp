# 日志

## v1.73.79 Auto-Repair 修复 + HTTP fallback 修复 (2026-06-15)

### 问题背景
1. **`loadPrompts is not a function`**: 用户升级到 v1.73.78 后，Quest/Agent 模式初始化时报错
   `Provider initialization failed: (0, import_core4.loadPrompts) is not a function`
2. **安装包文件未找到**: 用户尝试下载更新时提示"安装包文件未找到"

### 根因分析

#### loadPrompts 问题
- `build-main.mjs` 注入的 `__safeRequireCodepilot` (v5) 在模块缺失/损坏时返回 `{}`
- `quest-ipc.ts`/`agent-ipc.ts` 静态 import `loadPrompts`，模块加载时已绑定到 `{}`，`loadPrompts === undefined`
- repair 失败后无兜底，app 白屏不可用
- 附带问题：`main.ts` bootstrap 路径使用 `dirname(process.resourcesPath)` 而非 `process.resourcesPath`

#### 安装包未找到问题
- `/api/latest` 返回相对 URL（如 `/downloads/1.73.79/...`）
- `update-ipc.ts` HTTP fallback 直接传给 Node.js `fetch()`，Node.js fetch 不接受相对 URL

### 修复内容

| 文件 | 修改 | 说明 |
|------|------|------|
| `build-main.mjs` | v5→v6 | 添加 `__stub` 兜底模块（含 noop loadPrompts），修复失败时返回 stub 而非 `{}` |
| `main.ts` | 路径修正 | `dirname(process.resourcesPath)` → `process.resourcesPath` |
| `update-ipc.ts` | URL 补全 | 相对 URL 前加 `https://ide.zhejiangjinmo.com` |
| `electron-builder.json` | output | `release-v17377` → `release-v17379` |
| `core/package.json` | 版本 | `1.73.78` → `1.73.79` |
| `electron/package.json` | 版本 | `1.73.78` → `1.73.79` |

### 构建产物
| 平台 | 文件 | 大小 |
|------|------|------|
| Windows Portable | `LingJing-Portable-1.73.79-win-x64.exe` | 194.7MB |
| Windows Setup | `灵境 Setup 1.73.79.exe` | 195.0MB |

### 部署操作
1. ✅ **Git 提交** - `e3ee94d` - fix: v1.73.79
2. ✅ **Windows 构建** - electron-builder v25.1.8
3. ✅ **上传安装包**到 `/var/www/downloads/1.73.79/`
4. ✅ **更新 latest.yml** - 指向便携版 + hex sha512
5. ✅ **更新 versions.json** - latest→1.73.79, 添加 v1.73.79 条目
6. ✅ **PM2 重启** - lingjing-update-server + update-server
7. ✅ **Git 推送** - production + ide-origin + build-machine
8. 🔄 **Linux 构建** - 构建机 192.168.1.9 后台运行中

### 验证结果
```
/api/latest → {"version":"1.73.79","status":"published"} ✅
latest.yml → 200 ✅
Windows Portable → 200 (194.7MB) ✅
__stub fallback → injected in main.js ✅
__codepilot_dist__ backup → auto-generated in build ✅
```

### 涉及文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `build-main.mjs` | 修改 | v5→v6 stub 兜底 |
| `main.ts` | 修改 | 路径修正 |
| `update-ipc.ts` | 修改 | 相对 URL 补全 |
| `electron-builder.json` | 修改 | output 目录 |
| `core/package.json` | 修改 | v1.73.79 |
| `electron/package.json` | 修改 | v1.73.79 |
| `/var/www/downloads/1.73.79/` | 新增 | Windows 安装包 |
| `/var/www/downloads/latest.yml` | 更新 | v1.73.79 |
| `/var/www/html/versions.json` | 更新 | latest→1.73.79 |

## v1.73.78 账号系统全面修复 (2026-06-15)

### 问题背景
1. 管理后台用户管理页面无数据、无法添加用户
2. 桌面端IDE新版本需要登录，但无账号可用

### 根因分析

#### 问题1：管理后台 API 路由不匹配
- 管理后台 Vue SPA 使用 `axios.create({baseURL: "/admin/api"})` 
- Nginx 重写规则: `/admin/api/users` → `/api/admin/users`
- 但后端 `admin-api.js` 的 user 路由注册在 `/api/users`（无 admin 路径段）
- 导致管理后台调用 `GET /admin/api/users` 时请求被转发到 `/api/admin/users`，后端无此路由

#### 问题2：桌面端无法登录
- 桌面端本地数据库 `database.ts` 的 schema 中没有 `users` 表
- `auth-service.ts` 使用了 `db.exec()` 但 sql.js 适配器的 `exec()` 返回 void（非返回查询结果集）
- `auth-store.ts` 的自动登录回调 `window.electronAPI.auth.login('admin', 'admin123')` 因无用户且数据库API不兼容而失败

### 修改内容

#### 服务端 (services/backend/admin-api.js)
| 修改 | 说明 |
|------|------|
| 将 login/check-password/change-password 抽为独立处理函数 | 解耦路由注册与业务逻辑 |
| 添加 `/api/login`、`/api/check-default-password`、`/api/change-password` 路由 | 在 `/api/` 路径下挂载（nginx rewrite 后 `/admin/api/*` → `/api/*`）|
| 添加 `/api/admin/users` 路由变体 (GET/POST/PUT/DELETE) | 向后兼容原有 `/api/users` |
| 修复 changePasswordHandler 嵌套 try 语法错误 | 恢复 cloud-server 启动 |

#### Nginx 配置
| 修改 | 说明 |
|------|------|
| `rewrite ^/admin/api/(.*) /api/admin/$1 break` | → `rewrite ^/admin/api/(.*) /api/$1 break` |
| 作用：`/admin/api/users` → `/api/users`（匹配后端实际路由）|

#### 桌面端 (desktop/electron)
| 文件 | 修改 |
|------|------|
| `src/auth/auth-service.ts` | 将所有 `db.exec()` 改为 `db.all()`（sql.js 适配器兼容） |
| 同上 | 添加 `ensureAuthTables()` 函数，自动创建 users/conversations/messages 表 |
| `src/ipc/auth-ipc.ts` | 登录失败且无用户时自动创建默认 admin 用户并重试 |
| 同上 | 导入 `saveDatabase` 以持久化新用户 |

### 验证结果
```
✅ Admin login → 200 (获取JWT Token)
✅ List users → 200 (返回4个用户)
✅ Create user → 200 (成功创建新用户)
✅ User signup → 201 (用户自助注册)
✅ User login → 200 (获取用户JWT)
✅ User profile → 200 (获取用户信息)
✅ Admin panel HTML → 200 (管理后台可访问)
```

### 涉及文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `services/backend/admin-api.js` | 修改 | 路由解耦+添加/api/变体+修复语法错误 |
| `desktop/electron/src/auth/auth-service.ts` | 重写 | db.exec→db.all+自动建表 |
| `desktop/electron/src/ipc/auth-ipc.ts` | 修改 | 自动创建默认admin用户 |
| `desktop/core/package.json` | 修改 | v1.73.77 → v1.73.78 |
| `desktop/electron/package.json` | 修改 | v1.73.77 → v1.73.78 |
| `/etc/nginx/sites-enabled/ide.zhejiangjinmo.com.conf` | 修改 | nginx rewrite规则修复 |
| `/root/cloud-server/admin-api.js` | 部署 | scp上传+pm2 restart |

### 版本发布信息更新 (2026-06-15)
- ✅ **versions.json**: latest → 1.73.78, releaseNotes 更新, versions 数组添加 1.73.78 条目, entries 添加下载链接
- ✅ **latest.yml**: 更新为 1.73.78 版本信息（含 sha512 校验和），electron-updater 可自动识别
- ✅ **下载目录**: `/var/www/html/downloads/1.73.78/` 已包含 portable + setup 安装包及 latest.yml

## v1.73.76 自动更新修复 - 完整部署完成 (2026-06-15)

### 问题背景
v1.73.76 的 __safeRequireCodepilot v4 修复代码已部署，但安装包文件从未上传到生产服务器，导致：
1. electron-updater 无法下载更新文件（404 Not Found）
2. `/api/latest` 返回 `version: "0.0.0"`（版本条目缺少 `status: "published"` 字段）

### 关键修复
1. ✅ **构建 Windows Portable EXE**（release-v17376/LingJing-Portable-1.73.76-win-x64.exe, 194MB）
2. ✅ **构建 Linux AppImage**（185MB）和 **Deb**（109MB）（electron-builder 跨平台构建）
3. ✅ **上传安装包**到 `/var/www/downloads/1.73.76/`
4. ✅ **更新 `/var/www/downloads/latest.yml`** - 修正 sha512 和下载 URL
5. ✅ **更新 `/var/www/downloads/latest-linux.yml`** - Linux 自动更新配置
6. ✅ **修复 versions.json** - 添加 `"status": "published"` 到 v1.73.76 条目
7. ✅ **同步到 update-server** - `/opt/lingjing/update-server/data/versions.json`
8. ✅ **修正 versions.json SHA512** - 与实际构建文件匹配

### 自动更新链路验证
```
electron-updater → https://ide.zhejiangjinmo.com/downloads/latest.yml → 200 ✅
                  → https://ide.zhejiangjinmo.com/downloads/1.73.76/LingJing-Portable-1.73.76-win-x64.exe → 200 ✅
安全检测 → https://ide.zhejiangjinmo.com/api/latest → {version: "1.73.76", status: "published"} ✅
Linux → https://ide.zhejiangjinmo.com/downloads/1.73.76/LingJing-1.73.76-linux-x86_64.AppImage → 200 ✅
```

### 涉及文件
| 文件 | 操作 | 说明 |
|------|------|------|
| `/var/www/downloads/1.73.76/*.exe` | 新增 | Windows 安装包 |
| `/var/www/downloads/1.73.76/*.AppImage` | 新增 | Linux AppImage |
| `/var/www/downloads/1.73.76/*.deb` | 新增 | Linux Deb |
| `/var/www/downloads/latest.yml` | 更新 | 修正 sha512 和 URL |
| `/var/www/downloads/latest-linux.yml` | 更新 | Linux 版本信息 |
| `/var/www/html/versions.json` | 更新 | 添加 status:published，修正 sha512 |

## v1.73.77 完整发布 - build + 部署 + 推送完成 (2026-06-15)

### 变更内容
- **__safeRequireCodepilot v5**: 将 auto-repair 提取为独立函数，在所有 MODULE_NOT_FOUND 失败时触发自动修复（不仅限 NEEDS_REPAIR）
- 版本号: `1.73.76` → `1.73.77`（desktop/core + desktop/electron）

### 构建产物
| 平台 | 文件 | 大小 |
|------|------|------|
| Windows Portable | `LingJing-Portable-1.73.77-win-x64.exe` | 195MB |
| Windows Setup | `灵境 Setup 1.73.77.exe` | 195MB |
| Linux AppImage | `LingJing-1.73.77-linux-x86_64.AppImage` | 255MB |
| Linux deb | `LingJing-1.73.77-linux-x86_64.deb` | 109MB |

### 部署操作
1. ✅ **Git 提交** - `011eb4a` - v1.73.77: __safeRequireCodepilot v5
2. ✅ **Windows 构建** - 本地 pnpm build + electron-builder (v25.1.8)
3. ✅ **Linux 构建** - 通过构建机 192.168.1.9 跨平台构建 (electron-builder v25.1.8)
4. ✅ **上传安装包**到 `/var/www/downloads/1.73.77/`
5. ✅ **更新 latest.yml / latest-linux.yml** - 修正 sha512 和 URL
6. ✅ **更新 versions.json** - 添加 v1.73.77 (status: published)
7. ✅ **同步到 update-server** - 3 路径同步
8. ✅ **PM2 重启** - lingjing-update-server + cloud-server
9. ✅ **Git 推送** - build-machine / production / ide-origin / GitHub（全部成功）
10. ✅ **自动更新链路验证** - 全部 200 OK

### 验证结果
```
/api/latest → {"version":"1.73.77","status":"published"} ✅
latest.yml → 200 ✅
latest-linux.yml → 200 ✅
Windows Portable → 200 (195MB) ✅
Linux AppImage → 200 (255MB) ✅
Linux deb → 200 (109MB) ✅
```

## 系统巡检 + 磁盘清理 (2026-06-15)

### 生产服务器健康检查
| 检查项 | 状态 |
|--------|------|
| Uptime | ✅ 14 天, load 0.03 |
| 内存 | ✅ 7.1GB, 已用 2.0GB, 可用 5.1GB |
| Swap | ✅ 2.0GB, 已用 774MB |
| Nginx 配置 | ✅ syntax ok |
| PM2 服务 | ✅ 6 个服务全部 online |
| 自动更新 :3002 | ✅ v1.73.77 |
| 自动更新 :8000 | ✅ v1.73.77 |

### 磁盘清理
- **清理前**: 148G 中已用 119G (**85%**)，仅剩 23G ⚠️
- **清理后**: 148G 中已用 67G (**47%**)，可用 75G ✅
- **释放空间**: ~52GB
- **清理内容**: 
  - 删除 v1.64~v1.73.75 全部历史安装包（~200+ 文件）
  - 删除 20 个失效符号链接
  - 保留: 1.73.76/、1.73.77/（最近 2 个版本）、配置文件
- **下载目录**: 54GB → 1.2GB

### 构建机清理
- 删除 5 个历史 release 目录 (v17363/v17364/v17372/v17374/v17375)
- 保留: release-v17376、release-v17377

### 下载页面修复
- 修复 `/var/www/downloads/downloads.js` 下载页面显示旧版本 (v1.73.68)
- **根因**: versions.json 使用对象格式 files，但 JS 期待数组格式 → 异常 → fallback
- **修复**: 新增 platformMap + typeMap 归一化 + getHref/getFileName 修复
- **已验证**: 所有平台 4 个文件映射正确 (win-x64_setup/portable, linux-x64_appimage/deb)

### 移动端版本更新 + APK 构建完成 (2026-06-15)
- 更新 mobile/app.json : version 1.73.46 → 1.73.77, versionCode 70 → 71
- 更新 mobile/package.json : version 1.73.46 → 1.73.77
- 提交: `ae6a4d5` - chore: bump mobile version to 1.73.77
- **构建机**: npm install (1086 packages ✅) → gradle assembleDebug (489 tasks, 1h11m + 1m20s ✅)
- **APK 文件**: `lingjing-mobile-1.73.77.apk` (181MB)
- **上传到**: `/var/www/downloads/1.73.77/lingjing-mobile-1.73.77.apk`
- **versions.json**: 添加 `android-apk` 条目
- **下载页面**: 修复 downloads.js 添加 `android-apk` → `android` 平台映射
- **同步**: update-server versions.json 同步 ✅
