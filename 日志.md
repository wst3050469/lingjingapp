# 开发日志

## 最近任务

### 2026-05-21：修复三个用户反馈问题 ✅
- **问题一：云同步断连**
  - 根因：`CloudSyncClient` WebSocket 无心跳机制，连接空闲后被 nginx/proxy 断开
  - 修复：`sync-client.ts` 添加 `_heartbeatTimer` 每30秒发送 ping
  - 文件：`packages/core/src/cloud/sync-client.ts`
    - 新增 `_heartbeatTimer` 属性
    - 新增 `_startHeartbeat()` / `_stopHeartbeat()` 方法
    - `connectWebSocket()` 的 `onopen` 中启动心跳，`onclose` 中停止
    - `disconnectWebSocket()` 和 `disconnect()` 中清理心跳定时器
  - 验证：Vite 构建通过 ✅
- **问题二：APK 下载 404**
  - 根因：版本号错误，`lingjing-mobile-v1.40.1.apk` 从未发布
  - 解决方案：正确的最新下载链接为 `https://ide.zhejiangjinmo.com/lingjing-mobile-v1.46.4.apk`（已验证 200 OK）
- **问题三：对话页面空白**
  - 根因：可能的 React 渲染异常未被捕获，或 electronAPI 桥接未加载
  - 修复：
    - `BottomPanel.tsx`：`ChatPanel` 外层包裹 `ErrorBoundary`
    - `SidebarContainer.tsx`：`ChatSidebar` 外层包裹 `ErrorBoundary`
    - `App.tsx`：添加 `electronAPI` 沙箱检测（console.warn 提示）
  - 验证：Vite 构建通过 ✅

### 2026-05-21 (续)：修复管理员登录401 + 版本审核工作流 ✅
- **问题1：管理员登录401**
  - 根因：`admin-api.js` 只存在于生产服务器 `/root/cloud-server/`，不在源码中；默认密码文件不存在
  - 修复：
    - 从生产服务器同步 `admin-api.js` 到本地源码（1480行）
    - 创建 `init-admin-password.mjs` 密码初始化脚本
    - 在生产服务器运行初始化，默认密码 `admin123`
  - 验证：`POST /api/admin/login` 返回 token ✅
- **问题2：版本审核工作流**
  - `admin-api.js` 已有完整实现：
    - `POST /api/versions` → 创建草稿（status: `draft`）
    - `POST /api/versions/:version/submit-review` → 提交审核（status: `pending_review`）
    - `POST /api/versions/:version/publish` → 发布（status: `published`）
  - **修复 `readVersionInfo()`**：只返回 `published` 版本，过滤 `draft` 和 `pending_review`
  - **修复 submit-review**：不再设置 `data.latest`，防止待审核版本被 `/api/latest` 暴露
  - 客户端 `update-ipc.ts` 已有双重检测（服务端 + 客户端）
  - 验证：创建草稿→提交审核→发布全流程通过 ✅
- **文件变更**：
  - 新增：`cloud-server/admin-api.js`（1480行管理API路由）
  - 新增：`cloud-server/init-admin-password.mjs`（密码初始化）
  - 修改：`cloud-server/server.js`（readVersionInfo过滤逻辑）
- **Git**：`main@b9be69b97` → 生产 server ✅（force push），GitHub 网络问题暂未推送
- **生产服务器部署**：PM2 cloud-server 已重启 ✅，管理员登录可用 ✅
