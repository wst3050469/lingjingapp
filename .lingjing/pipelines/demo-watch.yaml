name: 文件变更自动检测
id: demo-watch
triggers:
  - type: watch
    watch:
      paths: ["."]
      patterns: ["**/*.ts", "**/*.tsx"]
      events: ["change"]
      debounceMs: 3000
stages:
  - name: notify
    order: 1
    tasks:
      - name: echo
        command: echo "[Pipeline] 检测到文件变更"
